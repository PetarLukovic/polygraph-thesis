%% ----------------------------------------------------------------------------
% CVG SA/MA thesis template
%
% Created 03/08/2024 by Tobias Fischer
%% ----------------------------------------------------------------------------
\chapter{Method}
\label{chap:method}

% REVIEW NOTE (not rendered): this is now full prose, not a skeleton. Only one section
% (the Two-Tier Gate, 3.6) still has no real figure -- it uses a self-contained boxed
% placeholder (\missingfig, defined below) instead of \includegraphics, specifically so the
% document keeps compiling cleanly in Overleaf until we sketch that one together. Every other
% section points at real images already sitting under images/03_method/<stage>/ --
% replace/adjust those freely, the \includegraphics paths don't need to change if you just
% edit the files in place.
%
% Odometry/Keyframing (3.2) now has two real figures: a 3-up whole-trajectory raw-vs-keyframe
% view (same three sessions as clustering) and a zoomed inset on one real KITTI keyframe
% trigger. Generation script: /local/home/plukovic/.claude/jobs/7015ebb3/tmp/
% make_odometry_figs.py + make_odometry_zoom2.py -- not saved anywhere permanent, so if these
% need regenerating (different sessions, different zoom window) they'd need rewriting, though
% the approach (parse_tum_trajectory + a LaMAR-CSV parser for iOS/HoloLens raw odometry,
% align_to_gt's fit_gt_to_est_transform to bring raw into the keyframe frame, then
% ground_plane_axes for consistent orientation) is straightforward to redo.
%
% A second flag: a few equations below FORMALIZE a prose description from
% polygraph/docs/method.md into notation (survival-rate score in 3.4, the scale-consistency
% filter in 3.5, MST edge weights in 3.5) rather than being read directly off the exact source
% formula in code -- I only had verified exact code for the *thresholds/values*, not every
% underlying formula's precise form. Worth a direct check against
% src/retrieval/cluster_index.py and src/pose_estimation/edge_estimator.py before this is
% final, flagged again inline where it matters.
%
% The clustering figure (3.3) now uses three real clusters.jpg outputs (KITTI/HGE-HoloLens/
% HGE-iOS) straight from polygraph's own src/visualization/clusters.py -- real data, not a
% schematic, confirmed to be genuine pipeline output before copying in. The overview figure
% (images/03_method/01_overview/pipeline_overview.png) is still reused as-is from the polygraph
% project report -- consider a thesis-formatted redraw of its labels/fonts before submission.
\newcommand{\missingfig}[1]{%
  \begin{center}
  \fbox{\parbox{0.85\textwidth}{\centering\vspace{0.3cm}
  \textbf{[FIGURE NEEDED: sketch together]}\\[4pt]
  \small #1
  \vspace{0.3cm}}}
  \end{center}
}

This chapter describes Polygraph, the multi-session trajectory optimization pipeline this thesis
presents. \mysecref{sec:method-overview} states the problem formally and walks through, at a
high level, what happens to one keyframe and one cluster as they move through the pipeline.
\mysecref{sec:method-odometry}--\ref{sec:method-graph} then cover each pipeline stage in turn,
in the order data actually flows: keyframing, clustering, cluster matching, pose estimation,
the two-tier gate, and pose-graph optimization.

\section{Problem Statement and Pipeline Overview}
\label{sec:method-overview}

\subsection{Notation and Problem Statement}

Let $S = \{1, \dots, N\}$ index the $N$ sessions to be fused. Each session $s \in S$ is
tracked independently by a single-session visual(inertial) odometry backbone
(\mychapterref{chap:relatedwork} discusses several), producing a stream of raw per-frame poses
$T^{\mathrm{raw}}_{s,i} \in \SE{3}$, $i = 1, \dots, F_s$, expressed purely in session $s$'s own
local frame $\mathcal{F}_s$, drifting over time, and with no inherent relationship to any
other session's frame. \mysecref{sec:method-odometry} selects a keyframe subsequence
$K_s = (k_1 < k_2 < \dots < k_{|K_s|}) \subseteq \{1,\dots,F_s\}$; write $T_{s,j} :=
T^{\mathrm{raw}}_{s,k_j}$ for the pose of the $j$-th keyframe of session $s$. Everything downstream of
\mysecref{sec:method-odometry} operates purely at the granularity of the keyframe.

Define a graph $G = (V, E)$ with one vertex per keyframe in every session,
$V = \{ (s,j) : s \in S,\ j = 1,\dots,|K_s| \}$, and edges $E = E_{\mathrm{odom}} \cup
E_{\mathrm{intra}} \cup E_{\mathrm{cross}}$ of three kinds:
\begin{itemize}
  \item \textbf{Odometry edges} $E_{\mathrm{odom}}$: one per consecutive keyframe pair
    $(s,j),(s,j{+}1)$ within a session, with measurement $z_{(s,j),(s,j+1)} = T_{s,j}^{-1}
    T_{s,j+1}$ taken directly from the raw odometry (trusted, affected by drift). Never removed
    by outlier rejection.
  \item \textbf{Intra-session edges} $E_{\mathrm{intra}}$: loop-closure edges between two
    \emph{non-consecutive} keyframes of the \emph{same} session, confirmed by the same
    retrieval-and-verification pipeline as cross-session edges
    (\mysecref{sec:method-cluster-matching}, \mysecref{sec:method-pose-estimation}) but
    admitted directly, without the gate, since that session's position is already anchored
    by $E_{\mathrm{odom}}$ (\mysecref{sec:method-gate}).
  \item \textbf{Cross-session edges} $E_{\mathrm{cross}}$: between keyframes of two
    \emph{different} sessions, added only once a candidate match survives verification
    (\mysecref{sec:method-pose-estimation}) and is admitted by the gate
    (\mysecref{sec:method-gate}); measurement $\hat{z}_{(s,j),(s',j')} \in \SE{3}$ is the
    estimated relative pose between the two keyframes.
\end{itemize}
The connected components of $G$ partition $V$ into groups of sessions confirmed, with
corroborated evidence, that they would revisit each other. Sessions in different components do not have an
established relative pose. For every vertex $v = (s,j) \in V$, let $X_v \in \SE{3}$ be its
pose in the shared frame of its own component. The goal is to estimate $\{X_v\}_{v \in V}$, for
every component, by solving
\begin{equation}
  \label{eq:method-pgo-objective}
  \min_{\{X_v\}_{v \in V}} \;
  \sum_{(u,v) \in E} \rho_{uv}\!\Big( r(X_u, X_v, z_{uv});\, \Sigma_{uv} \Big)
  \;+\; \sum_{v \in V_0} \big\| X_v \ominus p_v \big\|^2_{\Sigma_{\mathrm{prior}}},
\end{equation}
where $r(X_u,X_v,z) = \mathrm{Log}\!\big(z^{-1} X_u^{-1} X_v\big) \in \mathbb{R}^6$ is the
$\SE{3}$ residual under the standard Lie-algebra logarithm map, $\Sigma_{uv}$ is the edge's
noise covariance, $\rho_{uv}$ a robust loss (identity for odometry edges, trusted and never
pruned; a Cauchy $M$-estimator for confirmed loop-closure edges, both intra- and
cross-session, or, in the final archival pass, a GNC-TLS weight; see
\mysecref{sec:method-graph}), and $V_0 \subseteq V$ is the set of per-component gauge-fixing
anchor vertices, one per session, each with prior pose $p_v$.

Two properties of \eqref{eq:method-pgo-objective} are what distinguishes Polygraph from an
offline formulation of the same problem. First, $V$ and $E$ grow monotonically online, one keyframe and one confirmed edge at a time, without the assumption that $S$ or any session's full trajectory is known in advance. Secondly, $V_0$ is never fixed up front. Every session receives its own gauge-fixing prior the moment it starts (as its own, momentarily disconnected, single-vertex component), and components merge,
but never split, only when a cross-session edge survives verification and the gate.

\subsection{Pipeline Walkthrough}

Before the detailed, per-stage treatment in the rest of this chapter, it is worth tracing what
actually happens to one keyframe and one cluster as they move through Polygraph, end to end.

The raw odometry stream of a session is thinned to keyframes as it arrives
(\mysecref{sec:method-odometry}). Each new keyframe is appended to that session's own
currently-open \emph{cluster}, a short, spatially bounded run of keyframes
(\mysecref{sec:method-clustering}). Clusters, not individual keyframes, are the unit
everything downstream reasons about: a cluster only becomes visible to the rest of the
pipeline once it \emph{closes}, at which point every one of its member keyframes has already
been assigned a global image descriptor.

The moment a cluster closes, it is matched against the already closed clusters of every other session through image retrieval (\mysecref{sec:method-cluster-matching}). Its keyframes vote
for their nearest neighbors elsewhere, and a short list of candidate cluster pairs falls out. As unverified hypotheses, not yet trusted. Then each pair of candidates is geometrically checked (\mysecref{sec:method-pose-estimation}). Only a handful of keyframe pairs from the two clusters are matched, triangulated, lifted to the metric scale with a monocular depth prior, and cross-checked against each other. A pair that survives this becomes one \emph{confirmed edge}, a single metric relative pose between two keyframes, one in each cluster.

A confirmed edge is not yet allowed into the shared pose graph unconditionally. If both
clusters belong to the same session, they commit immediately. That session's own position
is already fully known from its own sequential odometry, so this is just corroborating
evidence. If the two clusters belong to different sessions, the edge instead passes through
the two-tier consensus gate (\mysecref{sec:method-gate}), which decides, based on how much
prior cross-session evidence that session already has, whether this single piece of evidence
is enough to trust on its own or needs to be corroborated by another before it is allowed to
merge two session' components together.

Every edge that is ultimately admitted (intra or cross session) becomes a factor in the
pose graph of \eqref{eq:method-pgo-objective}, kept current by a cheap incremental solve after
every closed cluster and periodically refined by a more expensive batch solve that also prunes
edges that turn out to be outliers (\mysecref{sec:method-graph}). The result, once every
session has been processed, is one pose graph spanning every session shown, with corroborated
evidence, to revisit another, each session's own trajectory corrected wherever it overlaps
with the rest.

\begin{figure}[htb]
  \centering
  \includegraphics[width=\textwidth]{images/03_method/01_overview/pipeline_overview.png}
  \caption{Left to right: each session's raw per frame odometry is grouped online into
    spatially bounded clusters (Clustering). When a cluster closes, its keyframes vote against
    a retrieval index built from every other session's keyframes (Cluster Matching). A
    candidate pair then goes through joint pose estimation. Keyframe pairs are matched and
    reduced by translation-consensus voting into one confirmed edge. Pose graph optimization
    adds every confirmed edge as a loop closure: a cheap incremental Fast update runs
    continuously, and a periodic Full update re-solves the whole graph and prunes outlier
    edges.}
  \label{fig:method-overview}
\end{figure}

Unless stated otherwise, every numeric threshold quoted in this chapter is the production configuration used to generate this thesis's KITTI multi-session results, not necessarily the pipeline's own built-in code defaults, which are in several places more conservative; every threshold remains freely configurable per dataset. 

\section{Device Odometry and Keyframing} 
\label{sec:method-odometry}

Polygraph does not track cameras itself: its input is the raw per-frame pose stream
$T^{\mathrm{raw}}_{s,i} \in \SE{3}$ produced by an off-the-shelf single-session
visual(-inertial) odometry backbone: ORB-SLAM3 \cite{campos2021orbslam3}, VINS-Fusion
\cite{qin2019vinsfusionlocal,qin2019vinsfusionglobal}, or a device's own onboard visual-inertial
odometry (a HoloLens or a Boston Dynamics Spot robot, for the indoor deployments in
\mychapterref{chap:experiments}). This stream is locally accurate but drifts over time, and is
expressed only in session $s$'s own local frame $\mathcal{F}_s$, with no relationship to any
other session's frame.

A raw frame becomes a keyframe once it has moved far enough from the previous keyframe.
Writing $t_{s,i}, R_{s,i}$ for the translation and rotation components of
$T^{\mathrm{raw}}_{s,i}$, and $j$ for the index of the most recently selected keyframe, frame
$i$ becomes the next keyframe the first time
\begin{equation}
  \label{eq:method-keyframe-trigger}
  \big\| t_{s,i} - t_{s,k_j} \big\| \;>\; \tau_{\mathrm{trans}}
  \quad\text{or}\quad
  \angle\!\big(R_{s,k_j}^{-1} R_{s,i}\big) \;>\; \tau_{\mathrm{rot}},
\end{equation}
where $\angle(R)$ denotes the rotation angle of $R \in \SO{3}$. The production values used for
this thesis's KITTI results are $\tau_{\mathrm{trans}} = 5.0$m and $\tau_{\mathrm{rot}} =
28.6°$; every subsequent stage of the pipeline operates purely on the resulting keyframe
subsequence $K_s$, never on raw per-frame poses.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.310\textwidth]{images/03_method/02_odometry/odometry_kitti_00.jpg}
  \hfill
  \includegraphics[width=0.391\textwidth]{images/03_method/02_odometry/odometry_hge_hololens.jpg}
  \hfill
  \includegraphics[width=0.239\textwidth]{images/03_method/02_odometry/odometry_hge_ios.jpg}
  \caption{Raw odometry (thin line) against the selected keyframe subsequence (dots), for the
    same three sessions as \myfigref{fig:method-clustering}: KITTI sequence 00 (4541 raw
    frames $\to$ 690 keyframes), an HGE HoloLens session (1338 $\to$ 632), and an HGE iPad
    session (1503 $\to$ 727). The raw stream is aligned into the keyframe trajectory's own
    frame (Umeyama, via polygraph's own \texttt{align\_to\_gt}) before plotting, since the two
    are not natively expressed in the same frame.}
  \label{fig:method-odometry}
\end{figure}

\begin{figure}[htb]
  \centering
  \includegraphics[height=6cm]{images/03_method/02_odometry/odometry_kitti_00_zoom.jpg}
  \caption{The keyframe trigger of \eqref{eq:method-keyframe-trigger}, zoomed into 14
    consecutive raw frames of KITTI sequence 00: most raw frames fall below the
    translation/rotation threshold and are skipped, until one crosses it and is accepted as
    the next keyframe.}
  \label{fig:method-odometry-zoom}
\end{figure}

\section{Online Clustering}
\label{sec:method-clustering}

Each session's own \texttt{ClusterBuilder} groups its keyframe stream into spatially bounded
\emph{clusters}, entirely online and using only that session's own travel distance. No
global trajectory or cross-session information is needed at this stage. A cluster
$C = (k_{j_1}, \dots, k_{j_2})$ starts at keyframe $k_{j_1}$ and closes at the first later
keyframe $k_{j_2}$ satisfying both
\begin{equation}
  \label{eq:method-cluster-close}
  j_2 - j_1 + 1 \;\ge\; n_{\min}
  \quad\text{and}\quad
  \Big( \big\| t_{s,j_2} - t_{s,j_1} \big\| \;>\; d_{\max} \;\;\text{or}\;\; j_2 - j_1 + 1 =
  n_{\max} \Big),
\end{equation}
i.e.\ the cluster needs at least $n_{\min}$ keyframes \emph{and} either to have travelled more
than $d_{\max}$ from its own start pose or to have reached $n_{\max}$ keyframes outright.
Once $C$ closes, its own last $n_{\mathrm{ov}}$ keyframes seed the next cluster's initial
members, so consecutive clusters share keyframes and chain with no coverage gap between them.
The production values are $n_{\min}=6$, $d_{\max}=10$m, $n_{\max}=15$, $n_{\mathrm{ov}}=2$.

Every keyframe, at the moment it is added to a cluster, is also assigned one global image
descriptor $d_{s,k} = f_{\mathrm{MegaLoc}}(I_{s,k}) \in \mathbb{R}^D$
\cite{berton2025megaloc}, needed by cluster matching (\mysecref{sec:method-cluster-matching})
the moment the cluster closes. The output of this stage is a growing, per-session sequence of
closed clusters, each with its member keyframes and their descriptors.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.284\textwidth]{images/03_method/03_clustering/clusters_kitti_00.jpg}
  \hfill
  \includegraphics[width=0.359\textwidth]{images/03_method/03_clustering/clusters_hge_hololens.jpg}
  \hfill
  \includegraphics[width=0.297\textwidth]{images/03_method/03_clustering/clusters_hge_ios.jpg}
  \caption{Real clustering output across three very different capture settings: KITTI sequence
    00 (stereo, outdoor driving, 172 clusters), an HGE session captured on a HoloLens (indoor
    walking, 28 clusters), and an HGE session captured on an iPad (indoor walking, 33
    clusters). Each color segment is one cluster, closing according to
    \eqref{eq:method-cluster-close}.}
  \label{fig:method-clustering}
\end{figure}

\section{Cluster Matching (Retrieval)}
\label{sec:method-cluster-matching}

When a cluster $C$ closes, it is matched against every other eligible session's already-closed
clusters through learned image retrieval, before any geometric verification is attempted. Each
member keyframe $k \in C$ queries a retrieval index of other sessions' keyframe descriptors and
casts one vote for the parent cluster of each of its $k_{\mathrm{top}}$ nearest neighbours (by
cosine similarity of MegaLoc descriptors). Votes are pooled per candidate cluster $C'$ and
normalized into a \emph{survival rate}
\begin{equation}
  \label{eq:method-survival-rate}
  \sigma(C, C') \;=\; \frac{\#\{\, k \in C : C' \text{ received a vote from } k \,\}}{|C|},
\end{equation}
the fraction of $C$'s own keyframes that voted for $C'$ at all; a candidate must clear a
minimum survival-rate floor to be considered further. $C$'s own last two same-session clusters
are excluded from candidacy by construction (an adjacency guard): they share keyframes with
$C$ by construction, not because of a genuine revisit. Subject to the survival floor, the
$k_{\mathrm{clusters}}$ highest-scoring candidates per other session survive. Production values
are $k_{\mathrm{top}} = 5$, $k_{\mathrm{clusters}} = 2$. The output of this stage is a short
list of candidate cluster pairs $(C, C')$: unverified hypotheses, not confirmed matches.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.85\textwidth]{images/03_method/04_cluster_matching/cluster_matching_explainer.png}
  \caption{Cluster matching: a newly closed cluster's keyframes vote against the pool of
    already-closed clusters from every other eligible session, producing a short list of
    candidate cluster pairs.}
  \label{fig:method-cluster-matching}
\end{figure}

\section{Joint Pose Estimation}
\label{sec:method-pose-estimation}

Cluster matching only proposes candidate pairs; this stage verifies them geometrically, and is
the only stage that actually looks at image content and produces metric poses.

\subsection{Keyframe-Pair Selection}

Checking every keyframe in $C$ against every keyframe in $C'$ would be wasteful and
overly redundant. Instead, every cross keyframe-pair $(k,k') \in C \times C'$ is weighted by
descriptor similarity, and a maximum-spanning-tree (MST) over this complete bipartite
similarity graph selects the $k_{\mathrm{pairs}}$ highest-similarity, spatially diverse pairs to
actually verify. The MST property guarantees that every selected pair covers at least one
keyframe no earlier selected pair did, so the selected pairs spread across the cluster instead
of collapsing onto one dominant viewpoint. Production value: $k_{\mathrm{pairs}} = 10$.

\subsection{Per-Pair Verification}

Each selected pair $(k, k')$ goes through the same six-step verification:
\begin{enumerate}
  \item \textbf{Sparse matching}: XFeat feature extraction \cite{potje2024xfeat} followed by
    LighterGlue matching, a lighter, faster retraining of LightGlue
    \cite{lindenberger2023lightglue} distributed as part of the XFeat project, giving a
    correspondence set $M$.
  \item \textbf{Essential-matrix RANSAC} (via poselib) on $M$, giving a scale-free relative
    rotation $\hat{R}$ and translation direction $\hat{t}$ (unit norm) plus an inlier subset
    $M_{\mathrm{in}} \subseteq M$.
  \item \textbf{Triangulation} (DLT) of $M_{\mathrm{in}}$ under $(\hat{R}, \hat{t})$, giving
    scale-free 3D points $\{X_i^{\mathrm{tri}}\}$, correct only up to one unknown global scale.
  \item \textbf{Monocular metric depth lift}: Metric3D v2 \cite{hu2024metric3dv2} predicts a
    metric depth map for both images; back-projecting each matched keypoint through its own
    image's depth map and camera intrinsics gives metric 3D points $\{X_i^{\mathrm{depth}}\}$.
  \item \textbf{Scale-consistency filter}: for each correspondence $i$, the implied per-point
    scale $\hat{s}_i = \|X_i^{\mathrm{depth}}\| / \|X_i^{\mathrm{tri}}\|$ should agree across
    genuinely correct correspondences (which share one true global scale); correspondences
    whose $\hat{s}_i$ is inconsistent with the rest are discarded, keeping only points where
    the scale-free triangulated position and the metric depth-lifted position agree up to a
    consistent per-point scale.
  \item \textbf{PnP} (via poselib) of the surviving depth-lifted (metric) 3D points against
    their 2D correspondences in the other image, giving a metric relative pose
    $\hat{T}_{k,k'} \in \SE{3}$.
\end{enumerate}
A pair is rejected outright unless it clears $\ge n_{\mathrm{valid}} = 150$ scale-consistent
points, $\ge \rho_{\mathrm{inlier}} = 0.80$ RANSAC inlier ratio, and $\le
\epsilon_{\mathrm{ransac}} = 0.05$ RANSAC score.

% REVIEW NOTE (not rendered): step 5's exact formula (the ratio s_i and how "inconsistent" is
% decided) formalizes method.md's qualitative description -- confirm against the real
% scale-consistency-filter code in src/pose_estimation/ (edge_estimator.py or pose_estimator.py)
% before treating \eqref{eq:method-survival-rate}-style precision here as final; same caveat
% for the MST edge-weight definition just above (descriptor similarity is stated qualitatively
% in method.md, not given as an exact formula).

\subsection{Translation-Consensus Voting}

The surviving per-pair estimates $\{\hat{T}_1, \dots, \hat{T}_m\}$ (of the up to
$k_{\mathrm{pairs}}$ attempted) are combined by a translation-consensus vote rather than
trusted individually. Two estimates \emph{agree} if their implied translations differ by less
than $\tau_{\mathrm{vote}} = 1.5$m; the largest mutually agreeing subset (a maximum clique in
the pairwise-agreement graph) is averaged into one confirmed edge measurement $\hat{z}$ via a
chordal $\SE{3}$ mean: translations averaged arithmetically, rotations averaged by
projecting their arithmetic mean onto $\SO{3}$ (via SVD). A candidate pair of clusters only
becomes a confirmed edge once at least $n_{\mathrm{vote}} = 2$ independent per-pair estimates
agree; one uncorroborated estimate is never enough, and the candidate is otherwise dropped.
When multi-edge consensus is enabled (the production setting used throughout this thesis),
every individual agreeing estimate is \emph{also} added as its own extra graph edge alongside
the averaged one, giving the optimizer more redundant evidence per confirmed match.

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.85\textwidth]{images/03_method/05_pose_estimation/pose_estimation_explainer.png}
  \caption{Per-pair verification: matched keyframe pairs between two candidate clusters go
    through sparse matching, RANSAC, triangulation, metric depth lift, and PnP.}
  \label{fig:method-pose-estimation}
\end{figure}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.85\textwidth]{images/03_method/05_pose_estimation/pose_consensus_plot.png}
  \caption{Translation-consensus voting on one real KITTI candidate pair: each of the five
    MST-selected keyframe pairs is either a failed estimate or a translation distance along the
    corridor; the real \texttt{\_aggregate()} vote separates agreeing (kept) from
    disagreeing (outlier) estimates, and the dashed line marks the final chordal-mean edge.}
  \label{fig:method-pose-consensus}
\end{figure}

\section{The Two-Tier Consensus Gate}
\label{sec:method-gate}

A confirmed edge from \mysecref{sec:method-pose-estimation} is not admitted into the shared
pose graph unconditionally. This section describes the gate that decides whether, and when, it
is.

\subsection{Same-Session Edges Bypass the Gate}

An edge whose two clusters both belong to the same session commits directly to the graph,
no check required: that session's own position is already fully determined by its own
sequential odometry chain (\mysecref{sec:method-overview}'s $E_{\mathrm{odom}}$), so a
same-session loop closure ($E_{\mathrm{intra}}$) is simply extra corroborating evidence, not
new placement information.

\subsection{Anchor/Query Assignment}

For a confirmed cross-session edge between session $s$ and session $s'$, let $n(s)$ denote the
number of currently live (unpruned) cross-session edges already incident on $s$. The session
with more live cross-session edges plays \emph{anchor} (its current pose estimate is treated
as the trusted reference for this check), and the other plays \emph{query}, the side being
tested; ties (including neither session having any live edges yet) are broken arbitrarily but
deterministically. Whether the query session already has at least one live cross-session edge
decides which of the two tiers below the candidate is routed through.

\subsection{Tier 1: Corroboration Pool}

If the query session is still fully disconnected ($n(\text{query}) = 0$), there is no live
estimate yet worth checking a single candidate against. Instead, candidates targeting that
session are pooled, and checked only against each other: once a subset of at least
$m_{\mathrm{corrob}} = 2$ pooled candidates mutually agree (their implied world poses within
$\tau_{\mathrm{pool}} = 2.0$m / $10.0°$ of one another) \emph{and} are independent (their
source clusters on the anchor side at least $g_{\min} = 3$ clusters apart, so two candidates
are not simply the same retrieval mistake repeated), they commit together as a group and
jointly establish that session's first anchor, arriving at $\ge 2$ live cross-session edges at
once, never at exactly one. A candidate that never finds enough agreeing peers is dropped once
it has sat in the pool longer than $t_{\max} = 1$ day.

\subsection{Tier 2: Pending Queue}

If the query session already has at least one live cross-session edge, the candidate is
checked immediately against the query's current live pose estimate, rather than queued first.
The consistency threshold depends on how much existing evidence that session already has:
\begin{equation}
  \label{eq:method-consistency-table}
  \big(\tau_{\mathrm{trans}}(n), \tau_{\mathrm{rot}}(n)\big) \;=\;
  \begin{cases}
    (\infty, \infty) & n = 0 \quad\text{(bootstrap row, unreachable in Tier 2 by
      construction)} \\
    (10.0\text{m}, 20.0°) & n \ge 1,
  \end{cases}
\end{equation}
where $n = n(\text{query})$. If the pose the candidate implies is consistent with the query's
current estimate under \eqref{eq:method-consistency-table}, it commits immediately and never
touches the queue. If it fails, it is placed in the pending queue and re-checked against the
graph's estimate at every later full update (\mysecref{sec:method-graph}) until it either
passes, accumulates $r_{\max} = 3$ rejections, or ages out past $t_{\max}$ (whichever comes
first), at which point it is dropped for good.

\subsection{Worked Example}

Consider three cross-session candidates between sessions A and B, arriving in order. Both
sessions start with $n(\cdot) = 0$, so edge 1 (A$\leftrightarrow$B) ties for anchor, A wins
arbitrarily, B is query with $n(\mathrm{B})=0$: Tier 1. Its corroboration pool for B now has
one entry, below $m_{\mathrm{corrob}}=2$; nothing commits yet. Edge 2 (a different A cluster
$\leftrightarrow$ B cluster) routes the same way, and the pool for B now has two entries: if
they agree and are independent, both commit together right now, and B jumps straight to $2$
live cross-session edges. If they disagree, both stay pooled, waiting for a future third
candidate to pair with one of them; if none ever arrives, both simply age out and B is never
anchored via this pair. Assuming edges 1 and 2 did corroborate, a third A$\leftrightarrow$B
candidate (edge 3) now finds $n(\mathrm{B}) \ge 1$ (Tier 2) and is checked immediately
against B's live estimate under the $10.0$m/$20.0°$ bound of
\eqref{eq:method-consistency-table}: if consistent, it commits immediately (B now grows one
edge at a time, never in pairs again); if not, it lands in the pending queue for a later
re-check.

\missingfig{A state-diagram-style figure: disconnected query session $\to$ Corroboration Pool
$\to$ joint commit; already-anchored query session $\to$ immediate consistency check $\to$
commit or Pending Queue $\to$ commit/reject on re-check. This is the chapter's most
conceptually dense, least visual section, worth the most joint sketching time.}

\section{Pose Graph Optimization}
\label{sec:method-graph}

Every admitted edge (the sequential odometry chain, same-session loop closures, and
cross-session loop closures once past the gate) becomes a factor in
\eqref{eq:method-pgo-objective}. When a session starts, it becomes its own connected
component with its own gauge-fixing prior vertex in $V_0$; the first cross-session edge that
survives the gate merges its two components (rebasing the smaller component's frame into the
surviving one).

\subsection{Fast and Full Updates}

Two solves keep the graph current, at different costs and cadences:
\begin{itemize}
  \item \textbf{Fast update}: an incremental Bayes-tree solve (iSAM2 \cite{kaess2012isam2}),
    folding in every new keyframe vertex and every newly admitted edge after each closed
    cluster.
  \item \textbf{Full update}: a batch nonlinear least-squares solve over the whole graph,
    run every $n_{\mathrm{slow}} = 10$ closed clusters. For every loop-closure edge $e$
    (intra- or cross-session), let $r_e$ be its residual under
    \eqref{eq:method-pgo-objective}; $e$ is pruned if
    \begin{equation}
      \label{eq:method-outlier-prune}
      r_e \;>\; \kappa \cdot \mathrm{median}\big(\{ r_{e'} : e' \text{ same group as } e \}\big),
    \end{equation}
    with $\kappa = 3$ and \emph{group} meaning intra-session edges are judged against their own
    median, cross-session edges against theirs, so one session's tighter loop closures
    cannot drag down the bar for everyone else's. This step also drains the pending queue
    (\mysecref{sec:method-gate}) against the freshly re-solved estimate.
\end{itemize}

\subsection{End-of-Run Finalization}

Once every frame across every session has been processed, one more full update runs
unconditionally, not gated by the cluster-count trigger like the periodic ones, and
drains the pending queue once more. Only then do two final passes run, once each, over the
complete graph:
\begin{enumerate}
  \item \textbf{Inter-cluster pair consensus}: every session pair's independent edges are
    cross-checked against each other using each edge's \emph{pre-fusion local pose} (the
    raw odometry-derived pose captured when the edge was created, never touched by any
    optimization pass) rather than the graph's current estimate. This is deliberate:
    \eqref{eq:method-outlier-prune} is structurally blind to a group of edges that are
    individually plausible \emph{and} mutually self-consistent, since least-squares optimization
    simply bends the graph to satisfy the majority, producing small residuals against the
    graph's own (possibly wrong) current estimate no matter how wrong that estimate collectively
    is. Comparing against each edge's fixed, un-warped local pose instead gives a reference the
    group could not have already corrupted.
  \item \textbf{Final GNC-TLS solve}: one archival Graduated Non-Convexity truncated
    least-squares pass \cite{yang2020gnc} over the \emph{complete} graph, including edges
    already pruned by the full update, giving every loop-closure edge one last principled
    outlier weight and producing the pipeline's final pose estimate. It inherits the same
    blind spot as \eqref{eq:method-outlier-prune} for the same reason (it is still only
    weighting by residual against the graph's own current solve), which is exactly why the
    inter-cluster pair consensus step above has to run first.
\end{enumerate}
The end-of-run order is therefore fixed: full update (removes obvious single-edge outliers)
$\to$ drain the pending queue (last chance for anything still waiting) $\to$ inter-cluster pair
consensus (catches hidden, mutually self-consistent outlier groups) $\to$ final GNC-TLS solve
(final weights for everything that survives).

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.48\textwidth]{images/03_method/07_graph/graph_edges_before.png}
  \hfill
  \includegraphics[width=0.48\textwidth]{images/03_method/07_graph/graph_edges_after.png}
  \caption{One real KITTI session's cross-session loop-closure edges before and after a full
    update's outlier pruning (\eqref{eq:method-outlier-prune}): 32 kept, 21 pruned.}
  \label{fig:method-graph-edges}
\end{figure}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.7\textwidth]{images/03_method/07_graph/graph_solve.png}
  \caption{The pose graph solve itself.}
  \label{fig:method-graph-solve}
\end{figure}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.85\textwidth]{images/03_method/07_graph/slow_update_outliers.png}
  \caption{Residuals of every loop-closure edge at one full update, relative to
    \eqref{eq:method-outlier-prune}'s per-group median threshold: the same 32 kept / 21
    pruned split as \myfigref{fig:method-graph-edges}.}
  \label{fig:method-graph-outliers}
\end{figure}

\section{Summary}
\label{sec:method-summary}

Returning to \mysecref{sec:method-overview}'s problem statement: the output of this pipeline
is one pose graph spanning every session that has been shown, with corroborated evidence, to
revisit another, with each session's own trajectory corrected wherever it overlaps with the
rest. It is built up incrementally, one keyframe and one closed cluster at a time, with no
requirement that every session be collected before processing starts, and no session ever
designated a fixed reference in advance. \mychapterref{chap:experiments} evaluates this
pipeline across the datasets introduced in \mysecref{sec:relatedwork-2go}.

% REVIEW NOTE (not rendered): a few open items --
% (1) two figures still needed (\missingfig above, sections 3.2 and 3.6) -- these are the
%     ones most worth sketching together, per your plan to add/adjust images yourself now
%     that the prose is in place;
% (2) \eqref{eq:method-pgo-objective}'s master objective is referenced back into from 3.6/3.7
%     (rho_uv, Sigma_uv) -- if this gets restructured, check those back-references still read
%     correctly;
% (3) the two formalized-not-verified spots flagged inline (survival-rate formula in 3.4,
%     scale-consistency filter + MST edge weight in 3.5) are the ones most likely to need a
%     small correction once checked against src/retrieval/cluster_index.py and
%     src/pose_estimation/edge_estimator.py directly;
% (4) \label{chap:experiments} referenced in the closing line doesn't exist yet.
