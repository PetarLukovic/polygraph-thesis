%% ----------------------------------------------------------------------------
% CVG SA/MA thesis template
%
% Created 03/08/2024 by Tobias Fischer
%% ----------------------------------------------------------------------------
\chapter{Experiments}
\label{chap:experiments}

% REVIEW NOTE (not rendered, 2026-08-27): this is an OUTLINE pass, not a finished chapter.
% \mysecref{sec:experiments-datasets} is fully real: every fact in the table and every figure
% is generated directly from polygraph's own parsers/GT files (see the per-figure captions for
% the exact script/source), nothing schematic or placeholder. \mysecref{sec:experiments-main}
% and \mysecref{sec:experiments-ablation}, by contrast, are deliberately skeletons: real
% methodology per subsection (what is measured, against what, and why), but every
% \missingfig{} and every "% TODO table" marks a real result not filled in yet, pending 3.5's
% own review and a decision on which numbers/plots from the polygraph report actually belong in
% the thesis body vs. an appendix. Cross-checked against the live polygraph report
% (docs/report.html) for every real fact used here (session lists, camera names, split sizes,
% shared/non-shared GT frames) -- see each subsection for the specific source.

This chapter first introduces the six datasets this thesis evaluates on
(\mysecref{sec:experiments-datasets}), covering what each one actually looks like, which agent captured
it, and whether it can be used to test cross-session fusion at all. \mysecref{sec:experiments-main}
then reports Polygraph's own accuracy on each dataset, single-session and multi-session, against
published baselines where they exist. Finally, \mysecref{sec:experiments-ablation} isolates the
effect of individual design choices (which camera(s) to use, which matcher/depth combination,
where the wall-clock time actually goes) that the main results alone do not explain.

\section{Datasets}
\label{sec:experiments-datasets}

Six real localization/mapping benchmarks are used, none of them captured for this thesis: KITTI
\cite{geiger2012kitti}, HGE (HoloLens and iPad captures) from LaMAR \cite{sarlin2022lamar}, HYDRO
from CroCoDL \cite{blum2025crocodl}, Oxford RobotCar \cite{maddern2017robotcar,maddern2020rtk},
VBR \cite{vbr}, and EuRoC \cite{burri2016euroc}. Together they span four agents (wheeled car,
legged robot, handheld/head-worn AR device, aerial vehicle), indoor and outdoor scenes, and both
RGB and greyscale imagery.

A dataset is only useful for testing \emph{cross-session} fusion, as opposed to single-session
refinement alone, if it gives a way to check whether two sessions were really placed in the
correct relative position, that is, if it has ground truth expressed in one shared coordinate
frame across sessions, not just an internally-consistent ground truth per session. \mytabref{tab:experiments-datasets-overview}
marks this per dataset. \mysecref{sec:method-gate}'s two-tier gate and every ATE number in
\mysecref{sec:experiments-main} that references anchoring accuracy specifically depend on it.

\begin{table}[htb]
  \centering
  \footnotesize
  \renewcommand{\arraystretch}{1.05}
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llllll}
  \hline
  \textbf{Dataset} & \textbf{Agent} & \textbf{Env.} & \textbf{Camera(s)} & \textbf{Sessions} & \textbf{Multi-GT} \\
  \hline
  KITTI & car & outdoor & \texttt{image\_2} (1/4) & 11 (7) & No \\
  HGE (HoloLens) & AR headset & indoor/outdoor & \texttt{hetlf} (1/4) & 5 & Yes \\
  HGE (iOS) & AR phone & indoor/outdoor & \texttt{phone} (1/1) & 4 & Yes \\
  HYDRO & legged robot & indoor & \texttt{frontleft} (1/4) & 5 & Yes \\
  RobotCar & car & outdoor & \texttt{stereo\_centre} (1/1) & 4 (2 spl.) & Yes \\
  VBR & car & outdoor & \texttt{camera\_left} (1/2) & 8 & No \\
  EuRoC & MAV & indoor & \texttt{cam0} (1/2) & 11 (3 rm.) & Partial \\
  \hline
  \end{tabular*}
  \caption[Dataset overview]{Used/available cameras, agent, environment, session counts, and
    multi-session-GT eligibility per dataset. EuRoC's ``Partial'' means a shared GT frame within each
    of its three rooms, not across rooms (\mysecref{sec:experiments-euroc}).}
  \label{tab:experiments-datasets-overview}
\end{table}

\subsection{KITTI}
\label{sec:experiments-kitti}

\begin{figure}[htb]
  \centering
  \includegraphics[width=\textwidth]{images/04_experiments/01_datasets/dataset_kitti_grid.png}
  \caption[KITTI ground-truth trajectories]{GT trajectories of the 7 loop-closure KITTI
    sequences, each in its own independent frame (own start pose = origin), since no shared
    frame exists across sequences. Orange dot: start pose. Note sequence 00's and 07's real,
    if not GT-provable, spatial overlap, both are the same downtown loop.}
  \label{fig:experiments-kitti-gt}
\end{figure}

KITTI \cite{geiger2012kitti} is 11 outdoor driving sequences (00 through 10) recorded from an
instrumented car around Karlsruhe, using its left color camera (\texttt{image\_2}) for retrieval
and pose estimation, stereo for odometry generation. Every sequence is its own independent drive
with its own zeroed-out GT origin. Nothing in KITTI's own ground truth places two sequences in a
shared frame, so by the rule above KITTI is not multi-session-GT-eligible, even though two of its
sequences, 00 and 07, do physically revisit the same intersection (a fact Polygraph's own
retrieval and pose estimation can and does detect, forming a real cross-session loop closure
between them, just not one that KITTI's own GT can validate). Seven sequences (00, 02, 05, 06,
07, 08, 09) contain the loop-closure structure needed to test intra- and cross-session fusion at
all and are the ones evaluated in \mysecref{sec:experiments-main-kitti}. The remaining four are
single, non-revisiting drives.

\begin{figure}[htb]
  \centering
  \includegraphics[height=5.7cm]{images/04_experiments/01_datasets/dataset_hge_hololens.png}
  \hfill
  \includegraphics[height=5.7cm]{images/04_experiments/01_datasets/dataset_hge_ios.png}
  \caption[HGE ground-truth trajectories, both capture devices]{GT trajectories for both HGE
    capture devices, in the site's own shared frame, over a faint backdrop of the real site scan
    (a horizontal slice of the laser/photogrammetry point cloud at trajectory height). Left: 5
    HoloLens sessions. Right: 4 iPad/ARKit sessions, picked for real spatial overlap with the
    HoloLens sessions and with each other.}
  \label{fig:experiments-hge-gt}
\end{figure}

\subsection{HGE}
\label{sec:experiments-hge}

HGE is one physical site (part of the LaMAR benchmark \cite{sarlin2022lamar}, an ETH Zurich
building interior/exterior spanning several floors and an outdoor courtyard) captured twice, by
two different agents: 5 sessions from a HoloLens 2 (a head-worn AR headset, 4 onboard RGB
cameras, of which the front-left, \texttt{hetlf}, is used for the main results) and 4 sessions
from an iPhone/iPad running ARKit (a handheld AR device, one camera per session, aliased to a
single canonical name \texttt{phone} since ARKit assigns a new sensor id every session). Both
capture types share LaMAR's own site-registered GT frame, so both are multi-session-GT-eligible,
and the same site scan (a subsampled laser/photogrammetry point cloud) can back both figures
below to show real corridor and room layout behind the trajectories, not just bare lines.
One particularity is worth flagging up front. HoloLens and iPad footage differ enough in
resolution and optics (480$\times$640 against 1440$\times$1920, roughly 9 times the pixel count)
that the sparse matcher downsamples the higher-resolution side to the lower-resolution side's own
detail ceiling before matching, without which cross-device correspondences essentially do not
exist. \mysecref{sec:experiments-main-hge} runs both devices standalone and merged, at two
different cross-session edge-trust settings, to see what that merge actually buys each device.

\subsection{HYDRO}
\label{sec:experiments-hydro}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.85\textwidth]{images/04_experiments/01_datasets/dataset_hydro.png}
  \caption[HYDRO ground-truth trajectories]{GT trajectories of all 5 HYDRO sessions in the
    site's own shared frame, over the same kind of real site-scan backdrop as \myfigref{fig:experiments-hge-gt}.
    All 5 sessions end up in one connected component once cross-session edges are formed
    (\mysecref{sec:experiments-main-hydro}).}
  \label{fig:experiments-hydro-gt}
\end{figure}

HYDRO is 5 indoor sessions from CroCoDL \cite{blum2025crocodl}, captured by a Boston Dynamics
Spot quadruped walking the same building complex on different days (two sessions from
2023-11-24, two from 2024-05-17, roughly six months apart). Spot carries 4 onboard cameras
(front-left, front-right, and two wide-FOV side cameras roughly 178\textdegree{} apart). The
front-left camera (\texttt{frontleft}) is used for the main results, all four in
\mysecref{sec:experiments-ablation-cameras}'s camera-pairing ablation. Like HGE, HYDRO's GT is
registered to one shared site frame and the same real site-scan backdrop is available.

\subsection{RobotCar}
\label{sec:experiments-robotcar}

\begin{figure}[htb]
  \centering
  \includegraphics[width=0.49\textwidth]{images/04_experiments/01_datasets/dataset_robotcar_easy_osm.png}
  \hfill
  \includegraphics[width=0.49\textwidth]{images/04_experiments/01_datasets/dataset_robotcar_hard_osm.png}
  \caption[RobotCar ground-truth trajectories, both splits]{RTK GT trajectories for both
    RobotCar splits, restricted to each session's own RTK-usable stretch, over a real
    OpenStreetMap basemap of central Oxford. Left: easy split (3 daytime sessions). Right: hard
    split (2 daytime, 1 night session). Two sessions appear in both panels.}
  \label{fig:experiments-robotcar-gt}
\end{figure}

Oxford RobotCar \cite{maddern2017robotcar}, with the RTK-GPS ground truth release
\cite{maddern2020rtk}, is repeated traversals of the same central-Oxford route by an instrumented
car, recorded months apart under different weather, season, and lighting. The single centre
stereo camera (\texttt{stereo\_centre}) is used for retrieval and pose estimation. RTK GPS gives
every session a genuinely shared world frame, so RobotCar is multi-session-GT-eligible, and, since
it is street-level outdoor driving, its GT overlays cleanly onto a real street map rather than a
site scan. The figure below uses free OpenStreetMap tiles as backdrop instead of a laser scan
(RobotCar has no scan mesh in this codebase, unlike HGE/HYDRO). Two splits are drawn from four
distinct sessions, sharing two sessions between them: an \emph{easy} split (3 daytime sessions)
and a \emph{hard} split (2 daytime, 1 night session, with real heavy heading noise), specifically
to test single- and multi-session robustness under real day/night appearance change.

\subsection{VBR}
\label{sec:experiments-vbr}

\begin{figure}[htb]
  \centering
  \includegraphics[width=\textwidth]{images/04_experiments/01_datasets/dataset_vbr_grid.png}
  \caption[VBR ground-truth trajectories]{GT trajectories of all 8 VBR training routes used in
    this thesis, each in its own independent frame. Gold dot: start; red square: end. Route
    lengths span roughly 1 to 9\,km, and self-revisit structure (loops/crossings within one
    session) varies a great deal by route, from \texttt{colosseo\_train0}'s single large loop to
    \texttt{ciampino\_train0}'s dense, hub-like revisits.}
  \label{fig:experiments-vbr-gt}
\end{figure}

VBR \cite{vbr}, the Vision Benchmark in Rome, is a larger multi-agent benchmark (it also includes
handheld and multi-floor indoor captures). This thesis uses only its 8 outdoor, car-captured
training routes (\texttt{campus}, \texttt{ciampino} $\times 2$, \texttt{colosseo},
\texttt{diag}, \texttt{pincio}, \texttt{spagna} $\times$ variants), the same subset 2GO's own
paper evaluates on, using the left camera of its stereo pair (\texttt{camera\_left}). Unlike
KITTI/HGE/HYDRO/RobotCar, no two of these 8 sequences share physical space at all, so VBR is not
multi-session-GT-eligible and every VBR result in \mysecref{sec:experiments-main-vbr} is
single-session only.

\subsection{EuRoC}
\label{sec:experiments-euroc}

\begin{figure}[htb]
  \centering
  \includegraphics[width=\textwidth]{images/04_experiments/01_datasets/dataset_euroc_rooms.png}
  \caption[EuRoC ground-truth trajectories, by room]{GT trajectories of all 11 EuRoC sessions,
    grouped into their three room-local shared frames (no shared frame across rooms). Every
    session in a room overlaps heavily with every other session in that room, real revisit
    structure a Vicon room's small footprint all but guarantees.}
  \label{fig:experiments-euroc-gt}
\end{figure}

EuRoC \cite{burri2016euroc} is 11 short indoor flights of a micro aerial vehicle (MAV), Vicon- or
Leica-tracked, split across three physically distinct rooms: Machine Hall (5 sessions), Vicon
Room 1 (3 sessions), and Vicon Room 2 (3 sessions), using the first camera of its stereo rig
(\texttt{cam0}). Ground truth is registered to each room's own fixed motion-capture rig, so
sessions recorded in the \emph{same} room do share one frame, but the three rooms have no
relationship to one another, the same caveat as KITTI's independent sequences. EuRoC is therefore
multi-session-GT-eligible only within a room, never across all 11 sessions at once, and
\mysecref{sec:experiments-main-euroc} runs the three rooms as three separate multi-session
graphs for exactly this reason. EuRoC's own odometry is already extremely tight (among the
tightest of any dataset here), so its main results section reframes the comparison around
cross-session anchoring accuracy rather than raw ATE, which barely moves.

\clearpage

\section{Main Experiments}
\label{sec:experiments-main}

Every dataset below is run in two modes: single-session, where each session is refined
using only its own intra-session loop closures, and multi-session, which additionally
allows cross-session edges between different sessions once they pass the two-tier gate
(\mysecref{sec:method-gate}). Accuracy is reported as ATE (m) against ground truth, Umeyama-aligned
per session (\mysecref{sec:method-graph}'s own convention, no scale correction, since every
odometry backbone used here is already metric). Where a dataset is multi-session GT eligible
(\mytabref{tab:experiments-datasets-overview}), a second, ref-anchored error is also
reported per non-reference session, isolating how accurately a session was placed relative to the
rest of its connected component, from ATE's own within session drift. Each connected component's reference session is Umeyama-fit to its own GT once, and that same fixed transform, not an independent per-session fit, is applied to every other session's estimate before comparing it to its own GT. Published
baselines are included wherever a directly comparable number exists in prior work.

\subsection{KITTI}
\label{sec:experiments-main-kitti}

Both regenerated odometry backbones (ORB-SLAM3, VINS-Fusion), each in single- and multi-session
mode, across the 7 loop-closure sequences (\mysecref{sec:experiments-kitti}), against published
ORB-SLAM3, Maplab-BIN, Maplab-SP, and 2GO baselines. Cross-session edges only ever form between
00 and 07 (the only pair that revisits the same place), so every other sequence's multi-session
result is identical to its single-session one by construction. Since KITTI has no shared GT frame,
this section can only show that multi-session mode improves ATE where a real overlap exists, not
validate the cross-session alignment itself the way HGE/HYDRO/RobotCar can.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrrrr}
  \hline
  \textbf{Seq} & \textbf{Raw (ours)} & \textbf{ORB-SLAM3 (pub.)} & \textbf{Maplab-BIN} & \textbf{Maplab-SP} & \textbf{2GO} & \textbf{Single (ours)} & \textbf{Multi (ours)} \\
  \hline
  00 & 5.28 & 4.85 & 37.51 & 12.69 & 1.82 & \textbf{1.25} & \textbf{1.25} \\
  02 & 7.34 & 5.64 & 18.01 & 44.78 & 8.04 & \textbf{4.85} & \textbf{4.85} \\
  05 & 2.14 & \underline{0.95} & 7.44 & 7.98 & \textbf{0.92} & 1.02 & 1.02 \\
  06 & 2.07 & \underline{0.79} & 7.09 & 10.74 & \textbf{0.62} & 0.90 & 0.90 \\
  07 & 1.29 & 0.50 & 5.60 & 5.63 & \underline{0.41} & 0.46 & \textbf{0.33} \\
  08 & \underline{3.28} & 3.67 & 40.79 & 11.65 & 3.78 & \textbf{3.22} & \textbf{3.22} \\
  09 & \underline{2.72} & 3.22 & 10.23 & 11.32 & \textbf{1.84} & \textbf{1.84} & \textbf{1.84} \\
  \hline
  \end{tabular*}
  \caption[KITTI ATE, ORB-SLAM3 backbone]{KITTI ATE (m), ORB-SLAM3 backbone. ``Raw (ours)'' is
    our own regenerated ORB-SLAM3 run's raw odometry, a different number from ``ORB-SLAM3
    (pub.)'', the ORB-SLAM3 paper's own externally published result. Bold marks the best result
    in a row, underline the second-best.}
  \label{tab:experiments-kitti-orb}
\end{table}

Our multi-session output matches or beats 2GO's own published result on four of the seven
sequences (00, 02, 07, 08), ties it exactly on 09, and trails it on 05 and 06, both short,
low-drift sequences where raw odometry is already close to GT and there is comparatively little
for the pipeline to correct. Sequence 07 is the only row where multi-session mode itself makes a
difference (0.46\,m $\to$ 0.33\,m, single vs.\ multi), consistent with 00/07 being the only pair
that actually revisits the same place.

ORB-SLAM3 is non-deterministic, so \mytabref{tab:experiments-kitti-orb}'s ``Single (ours)''/
``Multi (ours)'' columns are themselves a mean over 5 independently regenerated odometry runs.
Every one of those 5 runs individually is in \mysecref{sec:appendix-kitti-runs}
(\mytabref{tab:experiments-kitti-orb-runs}), for full transparency about the run to run spread the
mean is hiding.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrrrr}
  \hline
  \textbf{Seq} & \textbf{Raw (ours)} & \textbf{VINS+PGO (pub.)} & \textbf{Maplab-BIN} & \textbf{Maplab-SP} & \textbf{2GO} & \textbf{Single (ours)} & \textbf{Multi (ours)} \\
  \hline
  00 & 13.06 & 4.20 & 11.13 & 11.39 & 1.58 & \textbf{1.43} & \underline{1.46} \\
  02 & 21.00 & 13.03 & 23.96 & 18.29 & 10.58 & \textbf{8.07} & \textbf{8.07} \\
  05 & 6.58 & 3.68 & 11.37 & 11.21 & \textbf{1.70} & \underline{1.94} & \underline{1.94} \\
  06 & 3.40 & 1.68 & 13.27 & 14.76 & 1.48 & \textbf{1.26} & \textbf{1.26} \\
  07 & 1.90 & 0.65 & 7.48 & 7.70 & \underline{0.46} & 0.56 & \textbf{0.44} \\
  08$^{*}$ & 10.03 & 10.01 & 17.04 & 14.27 & \textbf{4.03} & \underline{10.03} & \underline{10.03} \\
  09 & 7.69 & 7.71 & 15.97 & 12.31 & 4.66 & \textbf{3.32} & \textbf{3.32} \\
  \hline
  \end{tabular*}
  \caption[KITTI ATE, VINS-Fusion backbone]{KITTI ATE (m), VINS-Fusion backbone. Same 7
    sequences and the same ``Raw (ours)'' vs. ``VINS+PGO (pub.)'' distinction as
    \mytabref{tab:experiments-kitti-orb}. VINS-Fusion odometry is confirmed deterministic (5
    independent regenerations, bit-identical output), so no min-max range is needed here.
    $^{*}$Sequence 08 is a genuine detection miss for this backbone, zero intra- or
    cross-session edges found, so single-session and multi-session both equal raw odometry. Bold marks the best result in a row, underline the second-best.}
  \label{tab:experiments-kitti-vins}
\end{table}

VINS-Fusion's raw odometry starts from a substantially worse baseline than ORB-SLAM3's on every
sequence (13.06\,m vs.\ 5.28\,m on 00, for example), yet the pipeline ends up matching or beating
2GO on five of the seven sequences on this backbone (00, 02, 06, 07, 09), one more than on
ORB-SLAM3, losing only on 05 and the detection-miss sequence 08. Raw odometry quality alone does
not predict which backbone benefits most from cross-session fusion.

The real per-sequence trajectory comparison (raw odometry vs. single-session vs. multi-session
output, both backbones) is in \mysecref{sec:appendix-kitti-traj}.

\subsection{HGE}
\label{sec:experiments-main-hge}

HoloLens (5 sessions) and iOS (4 sessions), against native HoloLens/ARKit odometry, since
regenerated ORB-SLAM3/VINS-Fusion odometry failed on this dataset outright and no external
per-session baseline exists. Each session's ATE is tracked through four stages, standalone
single-session, standalone multi-session (each device run on its own), then mixed with the other
device once \texttt{min\_kf\_pairs} (the minimum shared keyframe pairs a cross-session edge needs
to be trusted) is set to 3 and again to 4. First three stages use exactly the same configuration file. While fourth is slightly tuned to extract better results. Ref-anchored error is tracked the same way, standalone versus both mixed settings, since a shared reference frame is what actually exposes placement
changes that flat ATE does not.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrrrr}
  \hline
  \textbf{Session} & \textbf{Device} & \textbf{Raw} & \textbf{Single} & \textbf{Multi} & \textbf{Mixed (kf3)} & \textbf{Mixed (kf4)} \\
  \hline
  \texttt{hl\_996} & HoloLens & 0.32 & \textbf{0.32} & \textbf{0.32} & \textbf{0.32} & \textbf{0.32} \\
  \texttt{hl\_139} (ref, A) & HoloLens & 0.53 & 0.51 & \textbf{0.43} & \textbf{0.43} & 0.44 \\
  \texttt{hl\_693} & HoloLens & 0.22 & \underline{0.22} & \underline{0.22} & \underline{0.22} & \textbf{0.21} \\
  \texttt{hl\_295} (ref, B) & HoloLens & 0.13 & \textbf{0.12} & \textbf{0.12} & 0.13 & \textbf{0.12} \\
  \texttt{hl\_568} & HoloLens & 2.09 & 2.09 & \textbf{0.30} & \textbf{0.30} & 0.31 \\
  \texttt{ios\_01-20} & iOS & 0.35 & \textbf{0.35} & \underline{0.36} & \underline{0.36} & \underline{0.36} \\
  \texttt{ios\_07-01} & iOS & 0.35 & \underline{0.33} & \underline{0.33} & \textbf{0.31} & \underline{0.33} \\
  \texttt{ios\_06-24} & iOS & 0.67 & 0.67 & 0.67 & \underline{0.63} & \textbf{0.56} \\
  \texttt{ios\_01-18} & iOS & 0.85 & \textbf{0.78} & \textbf{0.78} & 0.80 & \textbf{0.78} \\
  \hline
  \end{tabular*}
  \caption[HGE ATE progression, all 9 sessions]{ATE (m) through 4 stages, all 9 HGE sessions.
    Single/Multi are each device run standalone. Mixed (kf3)/(kf4) are the same session's value once all 9 sessions run together at \texttt{min\_kf\_pairs} 3 and 4. Bold marks the best result among Single/Multi/Mixed (kf3)/Mixed (kf4) per row, underline the second-best, no second place marked where the best is itself a tie.}
  \label{tab:experiments-hge-ate}
\end{table}

Flat ATE moves little past standalone multi-session for either device. HoloLens's biggest single
improvement is session \texttt{hl\_568}, 2.09 meters raw to 0.30 meters multi-session, and mixing
barely changes it further. \texttt{hl\_295} is the one honest regression along the way, 0.12 to
0.13 meters at \texttt{min\_kf\_pairs}\,=\,3, before recovering to 0.12 at 4. \texttt{ios\_01-20}
is the one session mixing never helps at all, its best result is standalone at 0.35 meters.

\begin{table}[htb]
  \centering
  \scriptsize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrrrrr}
  \hline
  & & \multicolumn{3}{c}{\textbf{Trans (m)}} & \multicolumn{3}{c}{\textbf{Rot (\textdegree)}} \\
  \textbf{Session} & \textbf{Device} & \textbf{Standalone} & \textbf{kf3} & \textbf{kf4} & \textbf{Standalone} & \textbf{kf3} & \textbf{kf4} \\
  \hline
  \texttt{hl\_996} & HoloLens & \textbf{0.39} & \underline{0.99} & 1.45 & \underline{0.57} & 0.68 & \textbf{0.54} \\
  \texttt{hl\_139} (ref, A) & HoloLens & -- & \textbf{1.32} & \underline{1.69} & -- & \underline{0.80} & \textbf{0.57} \\
  \texttt{hl\_693} (ref, mixed) & HoloLens & 0.26 & -- & -- & 0.58 & -- & -- \\
  \texttt{hl\_295} (ref, B) & HoloLens & -- & \underline{0.23} & \textbf{0.16} & -- & \underline{0.68} & \textbf{0.53} \\
  \texttt{hl\_568} & HoloLens & \textbf{0.30} & \underline{1.28} & 1.68 & \underline{0.89} & 1.19 & \textbf{0.83} \\
  \texttt{ios\_01-20} & iOS & \underline{0.66} & 0.70 & \textbf{0.54} & \textbf{0.75} & 0.93 & \underline{0.83} \\
  \texttt{ios\_07-01} & iOS & 0.73 & \textbf{0.43} & \underline{0.52} & 1.61 & \textbf{0.70} & \underline{0.71} \\
  \texttt{ios\_06-24} & iOS & 1.37 & \underline{0.70} & \textbf{0.60} & 2.76 & \underline{1.58} & \textbf{1.38} \\
  \texttt{ios\_01-18} (ref, iOS) & iOS & -- & \textbf{0.96} & \underline{1.06} & -- & \textbf{0.54} & \textbf{0.54} \\
  \hline
  \end{tabular*}
  \caption[HGE ref-anchored consistency, standalone groups merging into one]{Standalone anchors
    within each device's own reference (HoloLens splits into 2 components, \{139, 568, 996\} and
    \{295, 693\}; iOS is one component, referenced to \texttt{ios\_01-18}). Mixed (kf3)/(kf4)
    anchor all 9 sessions in 1 bridged component, referenced to \texttt{hl\_693}, so it goes from a
    real standalone value to being the reference once merged, while \texttt{hl\_139}/\texttt{hl\_295}/\texttt{ios\_01-18}
    go the other way. Bold marks the best of the up-to-3 real values per row, underline the
    second-best, translation and rotation ranked independently.}
  \label{tab:experiments-hge-anchor}
\end{table}

Merging is a genuine win for iOS placement and a real loss for HoloLens placement, in the same
table. Every iOS session improves by \texttt{min\_kf\_pairs}\,=\,4, one nearly halves, 1.37 to 0.60
meters, while every non-reference HoloLens session gets worse, up to five times its standalone
value. Flat ATE barely shows either direction since each session's own local trajectory changes
very little, a shared reference frame is what actually exposes it. The net average moves slightly
worse overall, iOS-standalone mean 0.92 meters to Mixed (kf4) mean 0.96 meters across all 8
non-reference sessions, so merging is not a clean win, it trades HoloLens's tight local consistency
for a real improvement in iOS's absolute placement and single component map.

The real per session trajectory comparison (native odometry vs. single-session vs. multi-session
output, both devices) is in \mysecref{sec:appendix-hge-traj}. \mysecref{sec:appendix-hge-anchor}
shows the ref-anchoring itself, visualized against the real site scan.

\subsection{HYDRO}
\label{sec:experiments-main-hydro}

Same structure as HGE (native Spot odometry as the only baseline, ATE plus ref-anchored error),
over the 5 HYDRO sessions. All 5 sessions end up in a single connected component once
cross-session edges are formed, so there is exactly one reference session and no group split to
report.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrrrr}
  \hline
  \textbf{Session} & \textbf{Group} & \textbf{Raw} & \textbf{Single} & \textbf{Multi} & \textbf{Ref-anch. trans (m)} & \textbf{Ref-anch. rot (\textdegree)} \\
  \hline
  \texttt{11-24\_11-13} & 1 (ref) & 0.17 & 0.14 & \textbf{0.11} & -- & -- \\
  \texttt{11-24\_11-26} & 1 & 0.44 & \underline{0.25} & \textbf{0.14} & 0.15 & 0.49 \\
  \texttt{11-24\_11-43} & 1 & \underline{0.24} & \underline{0.24} & \textbf{0.18} & 0.18 & 0.29 \\
  \texttt{05-17\_14-39} & 1 & \underline{0.18} & \underline{0.18} & \textbf{0.15} & 0.16 & 0.28 \\
  \texttt{05-17\_16-43} & 1 & 0.47 & \underline{0.36} & \textbf{0.16} & 0.18 & 0.29 \\
  \hline
  Total (L2-norm, $n{=}5$) & -- & 0.73 & 0.55 & 0.34 & 0.17 (mean) & 0.34 (mean) \\
  \hline
  \end{tabular*}
  \caption[HYDRO ATE and ref-anchored error]{HYDRO ATE (m), all 5 Spot sessions, one connected
    component. Session identifiers abbreviated to capture date and time. ``(ref)'' marks the
    session the component's ref-anchored error is measured against. Bold marks the best result
    among Raw/Single/Multi in a row, underline the second-best. Ref-anchored columns are not
    ranked.}
  \label{tab:experiments-hydro-ate}
\end{table}

HYDRO's improvement is spread across sessions rather than driven by one outlier. Session
\texttt{11-24\_11-26} goes from 0.44 meters raw to 0.14 meters multi-session. Session
\texttt{05-17\_16-43} goes from 0.47 meters to 0.16 meters. Both are close to a threefold
reduction. The other three sessions already sit close to GT and change less. All five sessions
form a single connected component, so ref-anchored error covers the whole dataset instead of being
split across separate clusters like HGE. It stays tight at 0.17 meters translation and 0.34
degrees rotation on average.

The real per-session trajectory comparison (native odometry vs. single-session vs. multi-session
output) is in \mysecref{sec:appendix-hydro-traj}. \mysecref{sec:appendix-hydro-anchor} shows the
ref-anchoring itself, visualized against the real site scan.

\subsection{RobotCar}
\label{sec:experiments-main-robotcar}

Both backbones (VINS-Fusion stereo and RobotCar's own published visual odometry) run through the
full pipeline, not just compared as raw odometry, across both splits. RobotCar's drift is far
larger than the other datasets (tens of metres of single-session ATE even in daytime, worse at
night), so several thresholds tuned on HGE/HYDRO/KITTI needed real, evidence-based widening
specific to this dataset. That investigation, and a genuine backbone-dependent divergence on the
hard split, is summarized here and detailed in the appendix.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrr}
  \hline
  \textbf{Session} & \textbf{Raw} & \textbf{Single} & \textbf{Multi} & \textbf{Anchor trans (m)} & \textbf{Anchor rot (\textdegree)} \\
  \hline
  \multicolumn{6}{l}{\textit{VINS-Fusion}} \\
  \texttt{10-30\_13-52} (ref) & 76.84 & \textbf{37.41} & \underline{45.18} & -- & -- \\
  \texttt{10-30\_11-56} & \underline{83.34} & \underline{83.34} & \textbf{79.70} & 1122.98 & 129.86 \\
  \texttt{11-10\_11-55} & 53.68 & \underline{51.22} & \textbf{45.39} & 58.13 & 135.27 \\
  \hline
  Total (L2-norm, $n{=}3$) & 125.43 & 104.73 & 102.24 & 590.56 (mean) & 132.56 (mean) \\
  \hline
  \multicolumn{6}{l}{\textit{RobotCar VO}} \\
  \texttt{10-30\_13-52} (ref) & 186.88 & \underline{46.23} & \textbf{43.45} & -- & -- \\
  \texttt{10-30\_11-56} & \textbf{76.24} & \textbf{76.24} & \underline{78.46} & 1012.88 & 174.22 \\
  \texttt{11-10\_11-55} & \underline{121.26} & \underline{121.26} & \textbf{42.72} & 54.62 & 176.30 \\
  \hline
  Total (L2-norm, $n{=}3$) & 235.46 & 150.51 & 99.34 & -- & -- \\
  \hline
  \end{tabular*}
  \caption[RobotCar easy split, ATE and ref-anchored error]{RobotCar ATE (m), easy split (3
    daytime sessions), VINS-Fusion above, RobotCar's own published visual odometry (VO) below,
    same reference session for both. Session identifiers abbreviated to capture date and time.
    Bold marks the best result among Raw/Single/Multi in a row, underline the second-best;
    ref-anchored columns are not ranked.}
  \label{tab:experiments-robotcar-easy}
\end{table}

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrr}
  \hline
  \textbf{Session} & \textbf{Raw} & \textbf{Single} & \textbf{Multi} & \textbf{Anchor trans (m)} & \textbf{Anchor rot (\textdegree)} \\
  \hline
  \multicolumn{6}{l}{\textit{VINS-Fusion}} \\
  \texttt{10-30\_13-52} (ref) & \underline{76.84} & \textbf{59.23} & 214.51 & -- & -- \\
  \texttt{10-30\_11-56} & \textbf{83.34} & \textbf{83.34} & \underline{180.70} & 1072.27 & 159.57 \\
  \texttt{12-16\_18-44} (night) & 285.24 & \underline{247.06} & \textbf{212.83} & 216.69 & 149.34 \\
  \hline
  Total (L2-norm, $n{=}3$) & 306.94 & 267.38 & 352.08 & 644.48 (mean) & 154.45 (mean) \\
  \hline
  \multicolumn{6}{l}{\textit{RobotCar VO}} \\
  \texttt{10-30\_13-52} (ref) & 186.88 & \underline{34.71} & \textbf{33.34} & -- & -- \\
  \texttt{10-30\_11-56} & \textbf{76.24} & \textbf{76.24} & \underline{71.70} & 1402.76 & 171.62 \\
  \texttt{12-16\_18-44} (night) & 393.23 & \textbf{262.27} & \underline{262.51} & 459.96 & 174.27 \\
  \hline
  Total (L2-norm, $n{=}3$) & 442.00 & 275.32 & 274.14 & -- & -- \\
  \hline
  \end{tabular*}
  \caption[RobotCar hard split, ATE and ref-anchored error]{RobotCar ATE (m), hard split (2
    daytime, 1 night session), same reference and layout as \mytabref{tab:experiments-robotcar-easy},
    \texttt{11-10\_11-55} swapped for the night session \texttt{12-16\_18-44}. Bold marks the best
    result among Raw/Single/Multi in a row, underline the second-best; ref-anchored columns are
    not ranked.}
  \label{tab:experiments-robotcar-hard}
\end{table}

RobotCar is the first dataset here where multi-session fusion is not always the source of the
improvement. On the easy split, VINS-Fusion's gain comes almost entirely from single-session
refinement, 125.43 meters raw to 104.73 meters single, with multi-session mode adding only a small
extra reduction to 102.24 meters. VO benefits from both stages instead, 235.46 meters raw to
150.51 meters single, then a real $\sim$34\% further drop to 99.34 meters multi-session. Session
\texttt{10-30\_11-56} sits far off both backbones' ref-anchored estimate on this split, a genuine
weak-connectivity case, only 4 to 9 cross-session edges found for it against 20 to 29 for the
other two sessions, traced to a real 2.82\% raw drift rate on this session against 0.82--0.88\%
for the others, confirmed backbone-independent.

The hard split is a different story. Swapping in a night session breaks VINS-Fusion multi-session
mode outright, single-session is honestly the better result there (267.38 vs.\ 352.08 total), and
the reference session itself gets corrupted by fusion, 59.23 meters to 214.51. VO shows no such
regression, its total stays flat at 275.32 to 274.14 meters and its own reference session stays
protected, 34.71 to 33.34 meters. Its weaker sessions still sit further from their correct
relative position than VINS-Fusion's do, 1402.76 and 459.96 meters ref-anchored translation
against VINS-Fusion's 1072.27 and 216.69. Neither backbone reaches usable cross-session anchoring
once a night session is involved. Which shows limitation of learned pose estimation pipelines when handling mixture of night and day images. Which was the whole point of this investigation rather than tuning numbers to get the best results.

\subsection{VBR}
\label{sec:experiments-main-vbr}

Although campus \_train0 and campus \_train1 sessions \texttt{in} \texttt{campus\_train1} this dataset have been re-visited, we will run this dataset only in single-session mode in this table (\mysecref{sec:experiments-vbr}). Ran against our own regenerated VINS-Fusion odometry, against 2GO's own published per-sequence numbers (its own VINS+PGO result, two Maplab variants, and 2GO itself). VBR's sessions are an order of magnitude larger than KITTI's, 56 to 478 clusters per sequence, large enough that a
single blind pose-estimation threshold copied across every dataset in the repo caused real
retrieval starvation here specifically, only 22 of 67 clusters on \texttt{pincio\_train0} ever got
a retrieval candidate at all. Lowering the minimum valid-point count and doubling the retrieval
budget fixed that everywhere. A further per-sequence sweep of intra-session retrieval decay found
a clean split by route shape, dense hub-like routes with heavy appearance aliasing benefit from it
while sparse single-loop routes are hurt by it, so it stays enabled only on the sequences it
actually helps.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrrr}
  \hline
  \textbf{Seq} & \textbf{Raw (ours)} & \textbf{VINS+PGO (pub.)} & \textbf{Maplab-BIN} & \textbf{Maplab-SP} & \textbf{2GO} & \textbf{Single (ours)} \\
  \hline
  \texttt{campus\_train0} & 46.86 & 43.84 & 49.46 & 46.28 & \underline{12.30} & \textbf{11.86} \\
  \texttt{campus\_train1} & 38.40 & 23.40 & 44.43 & 34.93 & \underline{9.60} & \textbf{7.14} \\
  \texttt{ciampino\_train0} & 124.26 & 95.95 & 130.20 & 130.94 & \underline{22.84} & \textbf{21.97} \\
  \texttt{ciampino\_train1} & 39.66 & 34.68 & 40.94 & 40.79 & \textbf{14.20} & \underline{17.98} \\
  \texttt{colosseo\_train0} & 29.05 & 13.37 & 21.48 & 21.47 & \textbf{6.00} & \underline{7.66} \\
  \texttt{diag\_train0} & 36.24 & 33.10 & 27.17 & 27.23 & \underline{26.70} & \textbf{21.85} \\
  \texttt{pincio\_train0} & 13.30 & 7.75 & 11.22 & 11.23 & \textbf{3.18} & \underline{6.06} \\
  \texttt{spagna\_train0} & 21.71 & 13.22 & 20.83 & 20.35 & \textbf{3.73} & \underline{4.92} \\
  \hline
  Total (L2-norm, $n{=}8$) & 153.26 & 119.95 & 157.50 & 154.64 & \underline{41.71} & \textbf{39.93} \\
  \hline
  \end{tabular*}
  \caption[VBR ATE, VINS-Fusion backbone]{VBR ATE (m), VINS-Fusion backbone, all 8 training
    sequences. Aggregate (L2-norm) beats 2GO's own published floor, 39.93 against 41.71, the first
    VBR backbone where our reproduction beats 2GO overall; single-session already beats 2GO on
    \texttt{campus\_train0} (11.86 vs. 12.30) and \texttt{ciampino\_train0} (21.97 vs. 22.84). 2GO
    remains ahead on the other 6 rows, some by a wide margin (e.g.\ \texttt{pincio\_train0}, 6.06
    vs.\ 3.18). Bold marks the best result in a row, underline the second-best.}
  \label{tab:experiments-vbr-vins}
\end{table}

\subsection{EuRoC}
\label{sec:experiments-main-euroc}

Reframed around cross-session anchoring accuracy rather than raw ATE (\mysecref{sec:experiments-euroc}'s
own caveat, EuRoC's odometry is already too tight for ATE to move much). Each room group is run
as its own isolated multi-session graph, on both a stereo-inertial and a stereo-only ORB-SLAM3
backbone, and compared against ORB-SLAM3's own native multi-session mode (Atlas, sequential
map-merging via its own place recognition) on ref-anchored consistency and loop/merge-edge
accuracy, with a COVINS-G number included wherever its own paper reports a directly comparable
one. Machine Hall is reported two ways, the full MH01--05 run and MH01--03 as its own genuine
standalone run, not a subtotal read off the larger one.

EuRoC itself is not a good match for this pipeline. Its rooms are small and its own odometry is
already close to ground truth, exactly the regime where a dense SLAM system like ORB-SLAM3 has
the most to work with. The tables below are included for clarity, not as a claim that this
pipeline is the right tool for EuRoC. The real question they answer is how sparse, map-less pose
estimation compares to traditional dense 3D map estimation once both are pushed onto the same
small, high-overlap data.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrrr}
  \hline
  \textbf{Split} & \textbf{Ours trans (m)} & \textbf{Ours rot (\textdegree)} & \textbf{Ours edges} & \textbf{Atlas trans (m)} & \textbf{Atlas rot (\textdegree)} & \textbf{Atlas edges} \\
  \hline
  MH01--03 (standalone) & 0.153 & 1.11 & 11 & \textbf{0.052} & \textbf{0.80} & 592 \\
  MH01--05 & 0.231 & 1.20 & 43 & \textbf{0.112} & \textbf{1.17} & 915 \\
  V101--103 & 0.104 & 3.44 & 13 & \textbf{0.047} & \textbf{2.22} & 148 \\
  V201--203 & 0.068 & 2.30 & 11 & \textbf{0.038} & \textbf{0.88} & 65 \\
  \hline
  Avg (8 sessions) & 0.158 & 2.04 & -- & \textbf{0.077} & \textbf{1.36} & -- \\
  \hline
  \end{tabular*}
  \caption[EuRoC ref-anchored anchoring accuracy, ours vs. ORB-SLAM3 Atlas]{EuRoC trans./rot.\
    RMSE, mean over each split's non-reference sessions, ours vs. ORB-SLAM3's own native
    multi-session mode (Atlas), stereo-inertial backbone. MH01--03 is a genuine standalone run
    here, not a subtotal filtered from MH01--05. ``Ours edges'' = graph-committed cross-session
    cluster-pair constraints; ``Atlas edges'' = cross-session covisibility edges at weight
    $\geq 100$.}
  \label{tab:experiments-euroc-anchoring}
\end{table}

Atlas wins every split here, expected on data this favorable to dense mapping. Atlas also commits
far more edges to reach that result, 915 on MH01--05 against our 43, since it treats every
keyframe pair sharing map points as a candidate rather than filtering for genuinely useful ones
first. Isolating MH01--03 into its own standalone run, rather than reading it as a subtotal of the
MH01--05 run, moves the two systems in opposite directions. Ours gets worse on translation once
isolated, 0.153 meters against the subtotal's 0.071, despite rotation improving. Atlas gets better
on both once isolated, 0.052 meters and 0.80 degrees against the subtotal's 0.071 and 1.48.
MH04/05's extra cross-session data helps Atlas's own MH01--03 placement but actively hurts ours,
not the same direction for either system. The accuracy gap stays small, roughly 8 centimeters on
average and never more than 12 on any split, close to the pipeline's own typical per-pair pose
estimation error pooled across the other four datasets, around 0.40 meters median
(\mytabref{tab:ablation-pe-rejection}), even though EuRoC was not part of that ablation. EuRoC's
rooms are dense enough, typically only around 5 by 5 meters, that Atlas gets very accurate
loop-closure edges out of them, the same reason COVINS-G (\mytabref{tab:experiments-euroc-covins})
does well here too. Maplab will be added to this table at a later pass.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrr}
  \hline
  \textbf{Method} & \textbf{Front-end} & \textbf{\#Edges} & \textbf{Trans err (m)} & \textbf{Rot err (\textdegree)} \\
  \hline
  COVINS-G, PnP (MH01--05) & VINS-mono & 852 & 0.69 $\pm$ 0.60 & 3.44 $\pm$ 3.5 \\
  COVINS-G, 17-point (MH01--05) & VINS-mono & 1823 & \underline{0.10 $\pm$ 0.17} & \underline{1.09 $\pm$ 1.25} \\
  COVINS-G, PnP (MH01--05) & ORB-SLAM3 & 505 & 0.23 $\pm$ 0.32 & 1.56 $\pm$ 3.7 \\
  COVINS-G, 17-point (MH01--05) & ORB-SLAM3 & 406 & \textbf{0.07 $\pm$ 0.12} & \textbf{1.01 $\pm$ 0.9} \\
  Ours (MH01--05) & stereo-inertial & 43 & 0.48 $\pm$ 0.29 & 1.61 $\pm$ 0.96 \\
  Ours (MH01--05) & stereo & 69 & 0.53 $\pm$ 0.29 & 2.07 $\pm$ 1.40 \\
  \hline
  Ours (MH01--03, standalone) & stereo-inertial & 11 & 0.32 $\pm$ 0.09 & 1.10 $\pm$ 0.40 \\
  Ours (MH01--03, standalone) & stereo & 15 & 0.35 $\pm$ 0.18 & 2.14 $\pm$ 1.50 \\
  Ours (V101--103) & stereo-inertial & 13 & 0.29 $\pm$ 0.20 & 5.19 $\pm$ 2.43 \\
  Ours (V101--103) & stereo & 19 & 0.31 $\pm$ 0.17 & 5.55 $\pm$ 2.58 \\
  Ours (V201--203) & stereo-inertial & 11 & 0.22 $\pm$ 0.12 & 3.88 $\pm$ 1.64 \\
  Ours (V201--203) & stereo & 12 & 0.37 $\pm$ 0.21 & 5.51 $\pm$ 3.75 \\
  \hline
  \end{tabular*}
  \caption[EuRoC loop-edge accuracy vs. COVINS-G, both backbones]{Per committed cross-session
    edge, raw pre-optimization pose accuracy vs. GT. COVINS-G's own Table I caption restricts it
    to all 5 Machine Hall sequences, directly comparable to our MH01--05 rows only. Bold/underline
    mark the best/second-best jointly among the COVINS-G rows and the two MH01--05 rows above the
    rule; the rows below it have no COVINS-G number to rank against, shown for reference only.}
  \label{tab:experiments-euroc-covins}
\end{table}

Against COVINS-G's own published numbers, the picture is mixed. Its best configuration, the
17-point variant on either front end, beats us clearly on both translation and rotation. Its PnP
variant on VINS-mono is worse than us on both. Its PnP variant on ORB-SLAM3 sits close to us on
rotation and ahead on translation. Dropping IMU costs us here too, stereo-only roughly matches
stereo-inertial on translation but noticeably worsens rotation on every split, most on
V101--103, 5.55 degrees against 5.19. This shows that learned, mapless, pose estimation pipelines will have errors larger than more traditional dense map methods on average. For this reason, at locations such as this, where odometry errors are already marginal, and the map is very dense, it is very hard for our method to outperform traditional SLAM methods.

\section{Ablation Studies}
\label{sec:experiments-ablation}

Four design choices are isolated from the main results above: how many cameras to feed
retrieval and pose estimation, which sparse matcher and depth source to pair for pose estimation,
which image retrieval method proposes candidate pairs in the first place, and where the pipeline's
own wall-clock time actually goes.

\subsection{Camera Pairing}
\label{sec:experiments-ablation-cameras}

Production uses one camera per session (\mysecref{sec:experiments-datasets}'s ``Camera(s) used''
column). This ablation adds a second (and, for HGE/HYDRO, up to all four) onboard camera to
retrieval and pose estimation instead, on HGE, HYDRO, and KITTI, to see whether more viewpoints
per keyframe buys real accuracy or just more compute. Reports single-/multi-session ATE, intra-
and cross-session edge counts, connected components, and (HGE/HYDRO only, since KITTI has no
shared GT frame to anchor against) anchoring error, per camera combination. Full per-combination
tables for all three datasets are in \mysecref{sec:appendix-ablation-cameras}.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrrr}
  \hline
  \textbf{Dataset} & \textbf{Cameras (baseline $\to$ best)} & \textbf{Single (m)} & \textbf{Multi (m)} & \textbf{Cross edges} & \textbf{Edge mult.} \\
  \hline
  HGE & \texttt{hetlf} $\to$ all 4 & 0.652 $\to$ 0.652 & 0.280 $\to$ 0.278 & 264 $\to$ 458 & 1.7$\times$ \\
  HYDRO & \texttt{frontleft} $\to$ all 4 & 0.236 $\to$ 0.192 & 0.148 $\to$ 0.145 & 1690 $\to$ 3954 & 2.3$\times$ \\
  KITTI & \texttt{image\_2} $\to$ +\texttt{image\_3} & 2.702 $\to$ 2.691 & 2.689 $\to$ 2.677 & 98 $\to$ 96 & 0.98$\times$ \\
  \hline
  \end{tabular*}
  \caption[Camera-pairing summary, baseline vs.\ best]{Baseline (production) camera vs.\ the best
    camera combination per dataset, distilled from the full per-combination tables in
    \mysecref{sec:appendix-ablation-cameras}. Edge mult.\ is cross-session edges at the best
    combination over baseline, below 1 where they actually drop.}
  \label{tab:ablation-cameras-summary}
\end{table}

Here we provide a more concise table to show our findings. More cameras help most when they add genuinely new viewpoints. HYDRO's four cameras span nearly 360 degrees, and going from one to four cuts single-session ATE from 0.236 to 0.192 meters, with cross-session edges more than doubling from 1690 to 3954. HGE and KITTI barely move. HGE's multi-session ATE stays within 0.005 meters across all four camera combinations, and its anchoring error is actually worse with all four cameras than with just the baseline, 0.354 meters against 0.289 (\mysecref{sec:appendix-ablation-cameras}). KITTI's second camera is the other half of the same stereo rig, so it sees almost the same scene as the first, multi-session ATE only drops from 2.689 to 2.677 meters, and cross-session edges even drop slightly, 98 to 96. More viewpoints buy
real accuracy only when they are not mostly redundant with the one already in use.

\subsection{Pose Estimation Method}
\label{sec:experiments-ablation-pe}

Swaps \mysecref{sec:method-pose-estimation}'s production matcher (LightGlue) for LoFTR or MASt3R,
first on a large fixed sample of real candidate keyframe pairs per dataset (production thresholds,
not bare defaults), then as full single-/multi-session pipeline runs, further extended with two
matcher/depth-source variants and one fully native MASt3R variant, six methods in total. Full
detail (per-pair rejection breakdown, accuracy/timing, full-pipeline ATE, ref-anchored
consistency, wall time) is in \mysecref{sec:appendix-ablation-pe}.

Success rate does not predict accuracy. MASt3R succeeds on 91.8\% of HGE pairs against
LightGlue's 76.8\%, but its mean translation error on those successful pairs is almost twice as
high, 0.961 meters against 0.544. KITTI's own mean translation error, 44 to 50 meters across all
three methods, comes from a small number of severe outliers, the median stays under a meter for
every method there. None of this changes which method wins once the pipeline runs end to end, all
three land close together once outliers are absorbed into a real pose graph.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrr}
  \hline
  \textbf{Method} & \textbf{KITTI (m)} & \textbf{HGE (m)} & \textbf{HGE iOS (m)} & \textbf{HYDRO (m)} & \textbf{Wall, worst case} \\
  \hline
  LightGlue+M3D & \textbf{6.20} & \textbf{0.66} & \underline{1.13} & \textbf{0.34} & \textbf{1.00$\times$} \\
  LoFTR+M3D & \underline{6.58} & \underline{0.67} & 1.14 & \textbf{0.34} & 3.42$\times$ (iOS) \\
  MASt3R+M3D & 6.76 & \underline{0.67} & \textbf{1.11} & 0.35 & 4.08$\times$ (iOS) \\
  LightGlue+MASt3Rdepth & 12.04 & 0.68 & 1.14 & \textbf{0.34} & \underline{1.29$\times$ (iOS)} \\
  LoFTR+MASt3Rdepth & 10.98 & 0.68 & 1.14 & \textbf{0.34} & 4.01$\times$ (iOS) \\
  MASt3R-native & 26.34 & 2.19 & 1.19 & 0.73 & 4.25$\times$ (iOS) \\
  \hline
  \end{tabular*}
  \caption[Pose-estimation-method summary, all 6 methods]{Full-pipeline total ATE (L2-norm) per
    method and dataset, and the worst case wall time multiplier over LightGlue, distilled from the
    full per-sequence/session tables in \mysecref{sec:appendix-ablation-pe}. Bold marks the best
    result per column, underline the second-best, no second place marked where the best is itself
    a tie.}
  \label{tab:ablation-pe-summary}
\end{table}

The full pipeline erases most of the pair level differences on KITTI, HGE, and HYDRO, where
ref-anchored error stays close across all three original methods. HGE (iOS) breaks that pattern.
LoFTR still lands all four sessions in one connected component there, but two of its three
anchored sessions come out catastrophically wrong, tens of meters and around 45 degrees off, a bad
cross-session edge that stayed locally corroborated rather than a different graph split. LightGlue
and MASt3R both anchor sanely on the same data.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrr}
  \hline
  \textbf{Dataset} & \textbf{Method} & \textbf{Components} & \textbf{Mean ref-anch.\ trans (m) / rot (\textdegree)} \\
  \hline
  HGE & LightGlue+M3D & 2 & \textbf{0.31} / \underline{0.69} \\
  HGE & LoFTR+M3D & 2 & 0.38 / \textbf{0.67} \\
  HGE & MASt3R+M3D & 2 & \textbf{0.31} / \underline{0.69} \\
  HGE & LightGlue+MASt3Rdepth & 2 & 18.41 / 34.19 \\
  HGE & LoFTR+MASt3Rdepth & 2 & 18.47 / 34.32 \\
  HGE & MASt3R-native & 5 & 33.94 / 32.73 \\
  \hline
  HYDRO & LightGlue+M3D & 1 & \textbf{0.17} / 0.34 \\
  HYDRO & LoFTR+M3D & 1 & \textbf{0.17} / 0.33 \\
  HYDRO & MASt3R+M3D & 1 & 0.18 / 0.35 \\
  HYDRO & LightGlue+MASt3Rdepth & 1 & \textbf{0.17} / \textbf{0.32} \\
  HYDRO & LoFTR+MASt3Rdepth & 1 & 0.18 / \textbf{0.32} \\
  HYDRO & MASt3R-native & 4 & 14.37 / 44.53 \\
  \hline
  HGE (iOS) & LightGlue+M3D & 1 & \underline{0.92} / \underline{1.71} \\
  HGE (iOS) & LoFTR+M3D & 1 & 52.81 / 30.12 \\
  HGE (iOS) & MASt3R+M3D & 1 & 1.26 / 1.95 \\
  HGE (iOS) & LightGlue+MASt3Rdepth & 1 & \textbf{0.67} / \textbf{1.21} \\
  HGE (iOS) & LoFTR+MASt3Rdepth & 3 & 78.14 / 41.24 \\
  HGE (iOS) & MASt3R-native & 4 & 77.82 / 50.41 \\
  \hline
  \end{tabular*}
  \caption[Ref-anchored consistency, all 6 PE methods]{Same multi-session runs as
    \mytabref{tab:ablation-pe-kitti}. Each connected component's reference session is Umeyama-fit
    to GT, that fixed transform applied to the rest of the component, then measured against each
    session's own GT (same method as \mytabref{tab:experiments-hge-ate}/\mytabref{tab:experiments-hydro-ate}).
    Components is the number of connected components the multi-session graph settled into. Bold
    marks the best (smallest) result per dataset group and column, underline the second-best, no
    second place marked where the best is itself a tie.}
  \label{tab:ablation-pe-anchoring}
\end{table}

Wall time is where the original three methods otherwise separate. LoFTR and MASt3R take up to 3.42
and 4.08 times as long as LightGlue, worst case on HGE (iOS), for no accuracy gain and, in LoFTR's
case, an outright failure, which is why LightGlue stays the production default. The two
depth-source variants (\texttt{*+MASt3Rdepth}, matching MASt3R's own cross-view pointmap instead
of Metric3D) and the fully native \texttt{MASt3R-native} variant (skips the lifted-depth PnP stage
entirely) are worse almost everywhere too. MASt3R-native barely bridges sessions at all on HGE and
HGE (iOS), 5 and 4 disconnected components against production's 1 to 2 (table above), so its
apparently competitive HYDRO wall time, 1120 seconds against LightGlue's 1213, just reflects
skipping most of the cross-session matching work the others still do. LightGlue+MASt3Rdepth comes
closest to production, even edging it out on HGE (iOS) anchoring, 0.67 meters against 0.92, but it
breaks down on HGE HoloLens the same way LoFTR does on HGE (iOS). No depth-source swap beats
keeping Metric3D. Full per-sequence detail is in \mysecref{sec:appendix-ablation-pe}.

\subsection{Retrieval Method}
\label{sec:experiments-ablation-retrieval}

Swaps \mysecref{sec:method-cluster-matching}'s production retrieval method (MegaLoc) for BoQ, NetVLAD, or
SALAD, everything else unchanged, across all four locations (KITTI, HGE HoloLens, HGE iOS,
HYDRO). Reports single-/multi-session ATE and intra-/cross-session edge counts per method and
dataset. Full detail table is in \mysecref{sec:appendix-ablation-retrieval}.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} llrrr}
  \hline
  \textbf{Dataset} & \textbf{Best method} & \textbf{Single (m)} & \textbf{Multi (m)} & \textbf{Cross edges} \\
  \hline
  KITTI & MegaLoc (baseline) & 2.702 & 2.689 & 98 \\
  HGE (HoloLens) & NetVLAD & 0.651 & 0.279 & 308 \\
  HGE (iOS) & MegaLoc (baseline) & 0.532 & 0.531 & 56 \\
  HYDRO & MegaLoc (baseline) & 0.236 & 0.148 & 1690 \\
  \hline
  \end{tabular*}
  \caption[Retrieval-method summary, best method per dataset]{Lowest multi-session ATE per
    dataset across the four retrieval methods, distilled from the full per-method table in
    \mysecref{sec:appendix-ablation-retrieval}.}
  \label{tab:ablation-retrieval-summary}
\end{table}

MegaLoc, the production default, is already at or near the best achievable ATE on three of the
four locations. HGE HoloLens is the one place an alternative edges it out, and only barely, NetVLAD
reaches 0.651/0.279 meters against MegaLoc's 0.652/0.280, a difference well inside normal run to
run noise. HGE (iOS) is the more interesting result. BoQ, NetVLAD, and SALAD all find real
cross-session edges there, 14 to 22 of them, while finding almost none intra-session for NetVLAD.
Yet none of that turns into a multi-session accuracy gain, single and multi ATE stay within
0.01 meters of each other for all three. MegaLoc's own 56 cross-session edges are what actually
buys its multi-session improvement over single-session on that dataset. More candidate pairs are
not automatically useful ones, the pipeline's pose-estimation and consistency stages still have to
turn them into edges the graph can trust, and on HGE (iOS) only MegaLoc's edges clear that bar.

\subsection{Timing}
\label{sec:experiments-ablation-timing}

Additionally, we test full 11-session KITTI on wall-clock time, both backbones, both modes, on two GPUs (a Quadro RTX 6000 and an RTX 4090), broken down by pipeline phase (retrieval, pose estimation, graph optimization, other), plus a hardware-matched runtime comparison against 2GO's own published number.

\begin{table}[htb]
  \centering
  \scriptsize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrr}
  \hline
  \textbf{Metric} & \textbf{ORB-SLAM3, single} & \textbf{ORB-SLAM3, multi} & \textbf{VINS-Fusion, single} & \textbf{VINS-Fusion, multi} \\
  \hline
  Recording (s) & 2407.31 & 2407.31 & 2407.31 & 2407.31 \\
  Odom.\ gen.\ (s) & 2613.4 & 2613.4 & 954.0 & 954.0 \\
  Wall, Quadro (s) & 1189.64 & 1322.08 & 1076.28 & 1225.76 \\
  Wall, 4090 (s) & 756.3 & 819.6 & 695.1 & 910.1 \\
  Speedup & 1.57$\times$ & 1.61$\times$ & 1.55$\times$ & 1.35$\times$ \\
  RTF (Quadro / 4090) & 49.4\% / 31.4\% & 54.9\% / 34.0\% & 44.7\% / 28.9\% & 50.9\% / 37.8\% \\
  Retrieval (Quadro / 4090) & 0.31\% / 3.49\% & 1.32\% / 9.24\% & 0.34\% / 4.24\% & 1.38\% / 8.52\% \\
  Pose est.\ (Quadro / 4090) & 55.11\% / 53.98\% & 62.12\% / 51.56\% & 56.36\% / 48.14\% & 61.96\% / 51.42\% \\
  Graph opt.\ (Quadro / 4090) & 1.10\% / 0.84\% & 1.08\% / 0.79\% & 1.08\% / 0.78\% & 1.01\% / 0.66\% \\
  Other (Quadro / 4090) & 43.48\% / 41.69\% & 35.49\% / 38.41\% & 42.22\% / 46.84\% & 35.65\% / 39.40\% \\
  Desc.\ cache hit & 0.0\% (0/4055) & 0.0\% (0/4055) & 0.0\% (0/4043) & 0.0\% (0/4043) \\
  Depth cache hit & 63.0\% (1131/1796) & 64.1\% (1368/2134) & 64.6\% (1136/1758) & 65.4\% (1361/2082) \\
  \hline
  \end{tabular*}
  \caption[KITTI wall time and phase breakdown]{All 11 KITTI sequences, single invocation each.
    Wall time is real elapsed clock time including I/O. Odom.\ gen.\ is a one-time upstream cost,
    not re-run per invocation. All runs use \texttt{--fresh\_cache} flag, so cache-hit numbers reflect within-run reuse only.}
  \label{tab:ablation-timing-kitti}
\end{table}

Pose estimation dominates wall time on both GPUs, 48 to 62\% of the total, with retrieval and
graph optimization together under roughly 10\% in every configuration. The RTX 4090 cuts wall time
by 1.35 to 1.61 times over the Quadro RTX 6000 across all four backbone and mode combinations.
Multi-session mode adds a real cost over single-session, from a modest 8\% more wall time on
ORB-SLAM3 (RTX 4090) up to around 31\% on VINS-Fusion (RTX 4090), from processing the extra
cross-session candidate pairs.

\begin{table}[htb]
  \centering
  \footnotesize
  \begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} lrrrrr}
  \hline
  \textbf{Run} & \textbf{Odom.\ gen.\ (s)} & \textbf{Runtime (s)} & \textbf{Recording (s)} & \textbf{RTF} & \textbf{LC-edges} \\
  \hline
  \multicolumn{6}{l}{\textit{KITTI-00, VINS-Fusion odometry}} \\
  2GO paper (CL Absolute) & -- (not reported) & 387.16 & 469 & 82.5\% & 517 \\
  Ours, unbatched & 179.7 & 236 (227--254) & 470.6 & 50.2\% (48.2--54.0\%) & 142 \\
  Ours, batched (\texttt{batch\_depth}) & 179.7 & \textbf{209} (203--212) & 470.6 & 44.4\% (43.1--45.1\%) & 142 \\
  \hline
  \multicolumn{6}{l}{\textit{KITTI-00, ORB-SLAM3 odometry (no published 2GO number)}} \\
  Ours, unbatched & 501.3 & \textbf{234} (222--237) & 470.6 & 49.7\% (47.2--50.4\%) & 134 \\
  Ours, batched (\texttt{batch\_depth}) & 501.3 & 247 (213--252) & 470.6 & 52.5\% (45.3--53.6\%) & 134 \\
  \hline
  \multicolumn{6}{l}{\textit{\texttt{ciampino\_train0}, VINS-Fusion odometry (2GO's own slowest reported case)}} \\
  2GO paper (CL Absolute) & -- (not reported) & 2067.31 & 1549 & 133.5\% & 3403 \\
  Ours & 1791.0 & 586.57 & 1549 & 37.9\% & 34 \\
  \hline
  \multicolumn{6}{l}{\textit{\texttt{ciampino\_train0}, ORB-SLAM3 odometry (no published 2GO number)}} \\
  Ours & 2497.2 & 480.90 & 1549 & 31.0\% & 299 \\
  \hline
  \end{tabular*}
  \caption[Isolated-sequence runtime vs.\ 2GO, same GPU model]{Both systems on an RTX 4090. 2GO's
    own CL--Absolute variant is the directly comparable one, it only publishes a VINS-Fusion
    number for this sequence, so the ORB-SLAM3 group is our own extra data point. Runtime/RTF at
    KITTI-00 are median (min--max) across 5 repeat trials. Bold marks the faster of
    batched/unbatched per backbone at KITTI-00.}
  \label{tab:ablation-timing-2go}
\end{table}

On 2GO's own slowest reported case, \texttt{ciampino\_train0}, we finish in 586.57 seconds against
their 2067.31, three and a half times faster, while committing only 34 loop-closure edges against
their 3403. The same pattern holds on KITTI-00, 236 seconds against 387.16, with 142 edges against
517. Filtering candidate edges down to the robust ones before optimization is a large part of why
this pipeline is faster, not just a cleaner engineering choice.

The reported numbers are mean of 5 runs on KITTI-00 session over the same configuration and same odometry. We have also tested batching depth computation on KITTI-00 for different odometries but this gave us no significant speed increase over non batched computation. When using ORB-SLAM3 it even made the results slightly worse on average. The reason for this is to do with depth caching. This gives us the largest speed increase overall since most of the depth computation in later stages is cached rather than actually computed. Therefore, we need not compute enough depths per cluster pair for this to make significant difference. Of course, another big factor is actual number of edges we compute, which we keep at very low. 