%% ----------------------------------------------------------------------------
% CVG SA/MA thesis template
%
% Created 03/08/2024 by Tobias Fischer
%% ----------------------------------------------------------------------------

\vspace{3cm}

\chapter*{Abstract}
\noindent
Robots, vehicles, and wearable devices that repeatedly traverse the same environment --- a fleet of cars driving through the same city, or a person wearing an AR headset through the same building --- produce their own trajectory estimate from onboard visual or visual-inertial odometry. These per-session trajectories are locally consistent but drift over time and live in unrelated, session-specific coordinate frames; this drift becomes increasingly worse with the scale of the area traversed. Turning many such independent, drifting trajectories into one globally consistent set of trajectories, where different sessions revisit the same place, is the large-scale multi-session trajectory optimization problem this thesis addresses.

\noindent
Existing solutions to this problem largely operate offline: every session's data must be collected and available up front, one session is often designated in advance as a fixed reference frame, and only then is a single fused pose graph solved for. This assumption breaks down for a continuously operating deployment, where sessions arrive one at a time and there is no way to know in advance which one will end up anchoring the rest. In comparison to traditional SLAM methods, our approach operates entirely without a map: we optimize only trajectories and the absolute poses produced by learned pose estimation methods.

\noindent
This thesis presents Polygraph, a multi-session trajectory optimization pipeline that processes sessions online, one keyframe and one spatial cluster at a time, without assuming that all sessions are available up front or that any one session is in a common reference frame. Each session's odometry keyframes are grouped into local spatial clusters. This odometry may come from any source --- either an agent's own on-device odometry or any SLAM method it might be running. When a cluster closes, its keyframes are matched via learned image retrieval against every other session's keyframes, and candidate cross-session pairs are verified through sparse feature matching, monocular metric depth lifting, and robust pose estimation. A two-tier consensus gate admits a cross-session loop-closure edge into the shared pose graph only once independent evidence corroborates it. This guards against a single spurious match corrupting the whole graph, while the graph itself is kept up to date through a cheap incremental solve after every cluster and periodically refined through a robust, outlier-pruning batch solve. Our method is completely visual.

\noindent
We evaluate Polygraph across multiple different setups chosen to be as different from one another as possible: stereo-camera outdoor driving (KITTI \cite{geiger2012kitti}), HoloLens visual-inertial indoor traversal (LaMAR \cite{sarlin2022lamar}), iPad visual-inertial indoor traversal (LaMAR \cite{sarlin2022lamar}), and Boston Dynamics Spot visual-inertial indoor legged locomotion (CroCoDL \cite{blum2025crocodl}), among others. On the one hardware-matched, head-to-head comparison against the closest prior work we could run (the KITTI-00 sequence, identical RTX 4090), Polygraph runs about 3$\times$ faster (134.3s vs.\ 387.16s, a real-time factor of 28.5\%). On that same sequence, our method commits fewer loop-closure edges than the prior work \cite{lim2025twogo} (142 vs.\ 517) while achieving same or better accuracy. We test multi-device multi-session capabilities with further experiments and show algorithm robustness to different viewpoints, internal algorithm parameters, and different camera setups. Together, these results show that robust multi-session trajectory optimization is achievable in a genuinely online setting, without dense 3D reconstruction, across a
substantially wider range of real-world conditions than previously demonstrated.

% REVIEW NOTE (not rendered): numbers above (3x/2.9x, 134.3s/387.16s, 28.5% RTF, 142/517 edges)
% are still current per a 2026-08-27 re-check of polygraph/docs/report.html (now dated
% "last updated 2026-08-25") -- unchanged from before.
%
% Two things worth your own pass, not fixed here since they're content/precision calls, not
% grammar:
% (1) "achieving same or better accuracy" -- report.html's KITTI Table 1 (ORB-SLAM3 backbone)
%     now has real per-sequence ATE numbers: seq00 ours=1.25 vs 2GO=1.82 (better), seq02
%     ours=4.78 vs 2GO=8.04 (better), seq05 ours=1.02 vs 2GO=0.92 (slightly worse). So "same or
%     better" is roughly right but not universally true across every sequence -- consider
%     "comparable or better" or naming which sequences, if you want it airtight.
% (2) "Our method is completely visual" -- true for Polygraph's own cross-session reasoning
%     (retrieval, matching, depth are all vision-only), but the *input* odometry can be
%     visual-INERTIAL (uses IMU too, per the previous paragraph's own "on-device odometry or
%     any SLAM method" framing) -- worth a small wording check that this doesn't read as
%     contradicting that.
%
% Dataset status has also moved past what this abstract assumes: report.html's Experiments nav
% now lists KITTI, HGE, HGE (iOS), HYDRO, VBR, RobotCar, and EuRoC, all with real tables --
% RobotCar in particular is no longer "still being finalized" (that was true 2026-08-24, not
% anymore), and VBR is a new addition worth knowing about since it's 2GO's own dataset, so
% Polygraph is now tested head-to-head on territory 2GO itself already covered, not just new
% territory. None of this is touched in the prose above per your "just the abstract, minor
% fixes only" request -- flagging so it's not lost once you move on to the rest.
%
% If this comment block feels too heavy for the file, it's safe to delete once read -- you
% (via Overleaf) already deleted an earlier version of it once, which is fine.
